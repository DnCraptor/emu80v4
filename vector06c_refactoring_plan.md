# Вектор-06Ц для Murmulator

## Техническое задание и план рефакторинга

Версия: 0.85

Документ объединяет техническое задание и чек-лист выполнения работ.

История завершённых итераций и выпущенных патчей сохраняется в документе полностью; при добавлении новых итераций предыдущие записи не удаляются и не заменяются.

## Цель

Создать специализированную прошивку только для Вектор-06Ц.

### Основные требования

-   удалить мультиэмуляторность;
-   удалить стартовую страницу;
-   убрать динамическую загрузку конфигураций;
-   максимально упростить архитектуру;
-   убрать лишнюю виртуализацию;
-   обеспечить запуск без SD-карты;
-   использовать встроенные ROM;
-   оставить один физический видеорежим (800×600).

Работы выполняются в ветке **vector06c**.

## План работ

### Этап 1. Удаление мультиэмуляторности

-   [x] Удалить стартовую страницу.
-   [x] Удалить остальные платформы.
-   [x] Удалить динамическую загрузку конфигураций.
-   [x] Оставить только Вектор-06Ц.
-   [x] Перевести конфигурацию на статические структуры.

### Этап 2. Упрощение архитектуры

-   [x] Удалить лишнюю виртуализацию.
-   [x] Включить поля базовых классов в конечные структуры.
-   [x] Удалить ненужные подсистемы.
-   [x] Заменить косвенные вызовы прямыми.

### Этап 3. Видеотракт

-   [x] Оставить только режим 800×600.
-   [x] Удалить масштабирование.
-   [x] Удалить NumLock-режим.
-   [x] Удалить остальные физические видеорежимы.

### Этап 4. Меню

-   [ ] Иерархическое меню.
-   [ ] Общий файловый менеджер.
-   [ ] Snapshot.
-   [ ] Настройки.
-   [ ] ROM.
-   [ ] Носители.
-   [ ] Видео.
-   [ ] Звук.
-   [ ] Магнитофон.
-   [ ] Система.
-   [ ] О программе.


## Журнал выполнения

### Итерация 1 — прямой запуск Вектор-06Ц

-   [x] Удалён экран выбора платформы из `src/Main.cpp`.
-   [x] Удалены встроенный список платформ и CP866-таблица названий,
    использовавшиеся только стартовой страницей.
-   [x] В точку запуска жёстко передаётся `--platform vector`.
-   [ ] Остальные платформы и динамические конфигурации пока не удалялись: это
    отдельные последующие шаги этапа 1.

### Итерация 1.1 — исправление завершения стартового режима

-   [x] Восстановлено отключение временного framebuffer через
    `graphics_set_buffer(NULL, DISP_WIDTH, DISP_HEIGHT)` перед запуском PAL.
-   [x] Временный жёлтый буфер корректно отключён перед запуском PAL.
-   [x] Исправлен оставшийся на экране жёлтый прямоугольник, блокировавший
    отображение эмулятора после удаления меню.

### Итерация 1.2 — удаление временного framebuffer стартовой страницы

-   [x] Из `src/Main.cpp` полностью удалён `tmp_screen`, его выделение,
    заполнение жёлтым цветом и переключение framebuffer.
-   [x] `render_core()` теперь только инициализирует видеодрайвер; рабочий
    framebuffer устанавливает renderer Вектор-06Ц.
-   [x] Удалена скрытая зависимость `KorvetRenderer` от `tmp_screen`: пока
    Корвет остаётся в сборке, он снова выделяет и освобождает собственный
    framebuffer.

### Итерация 1.3 — единственный путь запуска Вектор-06Ц

-   [x] Из `src/Main.cpp` удалена искусственная передача `--platform vector`;
    запуск PAL теперь выполняется с единственным аргументом имени программы.
-   [x] `Emulation::processCmdLine()` больше не обрабатывает выбор платформы,
    старые платформенные ключи, `--conf-file` и выбор машины по расширению файла.
-   [x] Единственная создаваемая машина — `runPlatform("vector")`; штатная
    динамическая загрузка `vector.conf` пока сохранена до отдельного этапа.

### Итерация 1.4 — единственная зарегистрированная платформа

-   [x] Из `dist/emu80.conf` удалён каталог остальных платформ.
-   [x] Удалена отдельная платформа `vector.z80`: переключение 8080A/Z80 будет
    реализовано как настройка одной машины Вектор-06Ц.
-   [x] Удалены ассоциации расширений с платформами, утратившие смысл после
    фиксации единственной машины.
-   [x] В общем конфигурационном файле оставлена только регистрация
    `vector/vector.conf` с объектным именем `vector`.

## Архитектурные решения

### Процессор

-   8080A и Z80.
-   Переключение через меню.
-   После смены --- подтверждение, сохранение и рестарт виртуальной
    машины.

### Скорость CPU

-   Несколько фиксированных частот.
-   Unlimited.
-   Меняется без рестарта.

### Видео

-   Только 800×600.

### ROM

-   Штатные ROM встроены во flash.
-   Replacement ROM загружается с SD.
-   При отсутствии replacement используется встроенный ROM с
    предупреждением.

### Носители

-   2×FDD.
-   HDD.
-   Контроллеры отключаемые.
-   Образы можно извлекать.

#### Read Only

-   Галочка RO.
-   ScrLk переключает RO.
-   Повторное открытие уже используемого образа --- только RO.
-   Запись возвращает Write Protect.

### Магнитофон

-   Отключаемый.

### Звук

-   AY-3-8910 отключаемый.
-   Covox отключаемый.

### RTC

-   Начальное время = время сборки.
-   Далее часы идут программно.
-   Не сохраняется.

### Snapshot

-   Собственный формат.
-   12 слотов.
-   Left Win+F1..F12 --- сохранить.
-   Right Win+F1..F12 --- загрузить.
-   Полный дамп состояния устройств.
-   Без сжатия.
-   Блокирующее сохранение.
-   Во время операций: пауза, volume=0, VGA продолжает вывод.

### Меню

Разделы:

-   Процессор
-   Носители
-   ROM
-   Звук
-   Магнитофон
-   Снэпшоты
-   Видео
-   Система
-   О программе

### Горячие клавиши

-   Alt --- меню.
-   Alt+F3 --- быстрый доступ к универсальному загрузчику.
-   Alt+Del --- Soft Reset.
-   Ctrl+Alt+Del --- Hard Reset.
-   Left Win+F1..F12 --- сохранить snapshot.
-   Right Win+F1..F12 --- загрузить snapshot.


### Итерация 1.5 — прямой запуск конфигурации Вектора

- [x] Удалены выбор платформы по сохранённым настройкам и диалог выбора платформы.
- [x] `runPlatform()` принимает только имя `vector` и напрямую создаёт `vector/vector.conf`.
- [x] Общий конфигурационный файл больше не выбирает платформу: он только задаёт системные параметры и вызывает прямой запуск Вектора.
- [ ] Сам `vector/vector.conf` пока остаётся динамическим и требует файловой системы.

### Итерация 1.5.1 — восстановление системной инициализации

- [x] Возвращена обработка системной части `emu80.conf` перед запуском платформы.
- [x] Восстановлена инициализация `emulation.frequency`, частоты звука, FPS, VSync и сохранённых параметров из `emu80.opt`.
- [x] Устранён запуск видеотракта при неинициализированной частоте эмуляции.
- [ ] Системные параметры из `emu80.conf` предстоит перенести в статический код отдельной итерацией.


### Итерация 1.6 — очистка системного конфигурационного файла

- [x] Из `dist/emu80.conf` удалены `emu80.run`, `config.runFileName` и
  автоматическое подключение файла последней платформы.
- [x] Удалена оставшаяся регистрация платформы через `config.addPlatform`: Вектор
  уже запускается напрямую из `Emulation::processCmdLine()`.
- [x] Удалены закомментированные примеры запуска и создания других платформ.
- [x] Сохранены обязательные системные параметры, загрузка `emu80.opt` и вызов
  `emulation.processCmdLine`.

### Итерация 1.7 — отдельная базовая директория Вектор-06Ц

- [x] Базовая директория Pico PAL переключена с `/emu80/` на `/vector06c/`.
- [x] Начальная директория файлового диалога переключена с `/emu80` на `/vector06c`.
- [x] Исходный `dist/emu80.conf` восстановлен для полного мультиэмулятора.
- [x] Специализированный системный конфиг перенесён в `dist/vector06c/emu80.conf`.
- [x] Конфигурация и loader ROM Вектора скопированы в `dist/vector06c/vector/`.
- [ ] Динамическая загрузка конфигураций пока сохраняется; на этом этапе изменена только базовая директория.

### Итерация 1.8 — штатный loader ROM из flash

- [x] `Rom::Rom()` использует встроенный массив `vector_loader_rom` для пути `vector/loader.rom`.
- [x] Штатный loader Вектор-06Ц больше не читается с SD-карты.
- [x] Дублирующий `dist/vector06c/vector/loader.rom` удалён из специализированного дерева.
- [ ] `emu80.conf` и `vector.conf` пока остаются файловыми и всё ещё требуют SD-карту.

### Итерация 1.9 — встроенная конфигурация машины

- [x] `vector/vector.conf` встроен во flash как `src/pico/vector06c_config.h`.
- [x] Для Pico `Platform` передаёт встроенный текст непосредственно `ConfigReader`, без чтения файла с SD.
- [x] `dist/vector06c/vector/vector.conf` удалён; парсер конфигураций пока сохранён как промежуточный механизм.
- [ ] Следующий этап — заменить разбор встроенного текста статическим созданием объектов и связей.

### Итерация 1.10 — прямая системная инициализация

- [x] Удалён ошибочный `vector06c_system_config.h` с текстовой конфигурацией.
- [x] Обязательные системные параметры задаются напрямую в конструкторе `Emulation` без `ConfigReader`.
- [x] Напрямую установлены частота эмуляции, частота звука, громкость, FPS, VSync и параметры отладчика.
- [x] `processCmdLine()` вызывается непосредственно после системной инициализации.
- [x] Зависимость запуска от `emu80.conf` и `emu80.opt` отсутствует.

### Итерация 1.11 — удаление обработки командной строки при запуске

- [x] `Emulation::processCmdLine()` больше не разбирает параметры запуска, загрузки файлов и подключения носителей.
- [x] Удалена поддержка `post-conf`, утратившая смысл после отказа от динамических конфигураций.
- [x] Удалены командные параметры `run`, `load`, `disk-a`...`disk-d`, `hdd`, `edd` и `edd2` из пути создания машины.
- [x] Запуск теперь непосредственно создаёт единственную платформу `vector`.
- [x] Из `Emulation::setProperty()` удалены конфигурационные команды `runPlatform` и `processCmdLine`.

### Итерация 1.12 — прямой запуск из конструктора Emulation

- [x] Удалён промежуточный метод `Emulation::processCmdLine()`.
- [x] Конструктор `Emulation` непосредственно вызывает `runPlatform("vector")` после системной инициализации.
- [x] Из `Emulation.h` удалены объявление `processCmdLine()` и больше не используемый флаг `m_platformCreatedFromCmdLine`.

### Итерация 1.12.1 — восстановление создания платформы

- [x] После удаления `processCmdLine()` восстановлен прямой вызов `runPlatform("vector")` в конструкторе `Emulation`.
- [x] Вызов выполняется до `checkPlatforms()`, поэтому список платформ заполняется до проверки.
- [x] Устранён запуск видеотракта без создания и запуска машины Вектор-06Ц.

### Итерация 1.13 — удаление смены платформы и конфигурации

- [x] Из `Emulation::sysReq()` удалён обработчик `SR_CHPLATFORM`, создававший произвольную платформу через `EmuConfig`.
- [x] Удалён обработчик `SR_CHCONFIG`, перезапускавший машину через зарегистрированный список конфигураций.
- [x] Горячие команды смены платформы и конфигурации больше не могут вернуть мультиэмуляторный путь во время работы.
- [ ] `EmuConfig` пока сохраняется для существующего окна настроек и справки.

### Итерация 1.14 — единственная платформа без имени

- [x] `runPlatform()` больше не принимает имя платформы и безусловно создаёт Вектор-06Ц.
- [x] Удалена проверка имени `vector`, утратившая смысл в специализированной ветке.
- [x] `newPlatform()` пока сохранён для существующих внешних вызовов, но всегда пересоздаёт единственную машину.

### Итерация 1.15 — удаление внешнего API смены платформы

- [x] Удалён `Emulation::newPlatform()`: повторное создание машины по внешнему имени больше невозможно.
- [x] Удалён общий вызов `emuSelectPlatform()` и его объявление.
- [x] Qt- и WASM-обёртки больше не вызывают API выбора платформы.
- [x] Единственный путь создания Вектор-06Ц остаётся в конструкторе `Emulation`.

### Итерация 1.16 — прямая проверка создания Вектора

- [x] Удалён `Emulation::checkPlatforms()` — проверка списка платформ больше не используется.
- [x] Конструктор проверяет результат единственного вызова `runPlatform()` непосредственно.
- [x] При ошибке создания Вектора выводится сообщение и запрашивается завершение работы.
- [x] Удалено объявление `checkPlatforms()` из `Emulation.h`.

### Итерация 1.17 — единственная активная платформа

- [x] Удалён список `m_platformList`: экземпляр Вектор-06Ц хранится только в `m_activePlatform`.
- [x] Удалены `platformByWindow()` и `m_lastActivePlatform`; ввод, фокус и загрузка файлов работают напрямую с единственным окном Вектора.
- [x] Деструктор и `SR_CLOSE` напрямую удаляют единственную активную платформу.
- [x] `addChild()` сохранён как обязательный интерфейс `EmuObject`, но теперь принимает только единственную платформу.

### Итерация 1.18 — удаление одноразовой прослойки запуска

- [x] Удалён метод `runPlatform()`, использовавшийся только один раз при создании машины.
- [x] Единственный `Platform` Вектор-06Ц создаётся непосредственно в конструкторе `Emulation`.
- [x] Проверка наличия окна и обработка ошибки создания сохранены без промежуточного вызова.

### Итерация 1.19 — удаление зависимости от CmdLine

- [x] Конструктор `Emulation` больше не принимает и не хранит объект `CmdLine`.
- [x] Из `Main.cpp` удалено создание пустого `CmdLine`; `Emulation` создаётся напрямую.
- [x] Удалены неиспользуемые включения и предварительное объявление `CmdLine` из `Emulation`.
- [x] Файлы `CmdLine.*` и их записи в сборочных проектах удалены отдельной итерацией.

### Итерация 1.20 — удаление подсистемы CmdLine

- [x] Удалены `src/CmdLine.cpp` и `src/CmdLine.h`: после прямого запуска Вектора ссылок на класс больше нет.
- [x] Удалены записи `CmdLine.cpp/.h` из проектов Qt и Code::Blocks.
- [x] Pico CMake больше не включает `CmdLine.cpp` через общий `file(GLOB ...)`, поскольку файл удалён.

### Итерация 1.21 — удаление неподдерживаемых frontend-сборок

- [x] Удалены каталоги `src/qt`, `src/sdl`, `src/wx`, `src/wasm` и `src/lite`.
- [x] Удалены проекты Qt/Code::Blocks, старые desktop/WASM Makefile и desktop CI.
- [x] Удалён резервный `src/Main.bak`, относившийся к старой desktop-точке входа.
- [x] README сокращён до поддерживаемой Pico SDK/CMake-сборки специализированной прошивки.
- [x] Неподдерживаемые frontend-исходники больше не попадают под рекурсивный CMake glob и не требуют поддержания консистентности.

### Итерация 1.22 — статическое создание окна

- [x] `vector06c_config` подключается и используется безусловно.
- [x] Удалены `#ifdef PICO_RP2350`, неактуальный `#else` и файловый fallback.
- [x] `EmuWindow` создаётся непосредственно в `Platform` с фиксированными параметрами 800×600.
- [x] Блок окна удалён из встроенного и справочного `vector.conf`.

### Итерация 1.23 — статические RAM и ROM

- [x] Основная RAM объёмом 64 КиБ создаётся напрямую в `Platform`.
- [x] Loader ROM объёмом 32 КиБ создаётся напрямую в `Platform`.
- [x] `Rom` безусловно использует встроенный `vector_loader_rom` для `vector/loader.rom`.
- [x] Объявления `Ram ram` и `Rom rom` удалены из встроенного и справочного `vector.conf`.

### Итерация 1.24 — статическое создание CPU

- [x] `Cpu8080` создаётся непосредственно в `Platform`.
- [x] Частота 3 МГц и стартовый адрес `0x0000` задаются напрямую.
- [x] Объявление CPU и его базовые параметры удалены из встроенного и справочного `vector.conf`.
- [x] Оставшийся конфиг продолжает связывать объект `cpu` с адресными пространствами, ядром и ожиданиями.

### Итерация 1.25 — статические адресные пространства

- [x] `VectorAddrSpace` создаётся непосредственно в `Platform` и получает RAM, ROM и CPU без парсера.
- [x] Портовое `AddrSpace` создаётся непосредственно в `Platform`.
- [x] CPU напрямую подключается к обоим адресным пространствам.
- [x] Объявления адресных пространств и базовые связи удалены из встроенного и справочного `vector.conf`; связи с рендерером и RAM-диском пока остаются в конфиге.

### Итерация 1.26 — статические видеорендерер и ядро

- [x] `VectorRenderer` создаётся непосредственно в `Platform` и напрямую получает основную RAM.
- [x] Режим отображения видимой области задаётся прямым методом без конфигурационного парсера.
- [x] `VectorAddrSpace` напрямую подключается к рендереру.
- [x] `VectorCore` создаётся непосредственно и связывается с окном, рендерером и CPU.
- [x] Объявления рендерера и ядра и их базовые связи удалены из встроенного и справочного `vector.conf`.

### Итерация 1.26.1 — восстановление связи объектов с платформой

- [x] `Platform::addChild()` безусловно назначает добавляемому объекту текущую платформу.
- [x] Статически созданные CPU, адресные пространства, рендерер и ядро получают тот же `m_platform`, который ранее назначал `ConfigReader`.
- [x] Исправлен срыв эмуляции при первом кадровом событии `VectorCore::vrtc()`.

### Итерация 1.27 — статические клавиатура и ввод текста

- [x] `VectorKeyboard` создаётся непосредственно в `Platform`.
- [x] `VectorKbdLayout` создаётся непосредственно и без парсера переводится в режим QWERTY.
- [x] `KbdTapper` создаётся непосредственно; времена нажатия, отпускания и задержка после Enter задаются прямыми методами.
- [x] Объявления клавиатуры, раскладки и `KbdTapper` удалены из встроенного и справочного `vector.conf`.

### Итерация 1.28 — статический основной ВВ55

- [x] `VectorPpi8255Circuit`, магнитофонный `GeneralSoundSource`, основной `Ppi8255` и `AddrSpaceInverter` создаются непосредственно в `Platform`.
- [x] Обвязка ВВ55 напрямую связана с рендерером, клавиатурой и магнитофонным источником звука.
- [x] Основной ВВ55 напрямую подключён к диапазону портов `0x00..0x03`.
- [x] Соответствующий блок удалён из встроенного и справочного `vector.conf`.

### Итерация 1.29 — статический регистр палитры

- [x] `VectorColorRegister` создаётся непосредственно в `Platform`.
- [x] Регистр напрямую связывается с уже статическим `VectorRenderer`.
- [x] Регистр напрямую подключается к диапазону портов `0x0C..0x0F`.
- [x] Соответствующий блок удалён из встроенного и справочного `vector.conf`.

### Итерация 1.30 — статические дополнительные порты и звук

- [x] Второй `Ppi8255`, его `AddrSpaceInverter`, `VectorPpi8255Circuit2` и 7-битный `Covox` создаются непосредственно в `Platform`.
- [x] Covox переводится в отрицательную полярность прямым вызовом, а второй ВВ55 подключается к портам `0x04..0x07`.
- [x] `Pit8253` с частотой 1,5 МГц, его звуковой источник и инвертор портов создаются напрямую; таймер подключён к `0x08..0x0B`.
- [x] `Psg3910` с частотой 1,75 МГц и `Psg3910SoundSource` создаются напрямую; AY подключён к `0x14..0x15`.
- [x] Все три конфигурационных блока удалены из встроенного и справочного `vector.conf`.

### Итерация 1.31 — статическая дисковая портовая обвязка

- [x] `Fdc1793`, его инвертор адресов и `VectorFddControlRegister` создаются непосредственно в `Platform`.
- [x] ВГ93 подключён к портам `0x18..0x1B`, регистр выбора дисковода — к `0x1C`.
- [x] `AtaDrive` создаётся напрямую и переводится в геометрию Вектора без конфигурационного парсера.
- [x] `VectorHddRegisters` создаётся напрямую, связывается с ATA-устройством и подключается к портам `0x50..0x5F`.
- [x] Образы дискет и HDD остаются в конфиге и по именам подключаются к уже статическим контроллерам.
- [x] Соответствующие объявления контроллеров удалены из встроенного и справочного `vector.conf`.

### Итерация 1.32 — статические образы дискет и HDD

- [x] Образы дискет `A` и `B` создаются непосредственно в `Platform` с геометрией 80×2×5×1024.
- [x] Метки и файловые фильтры образов задаются прямыми вызовами.
- [x] Оба образа напрямую подключаются к статическому `Fdc1793`.
- [x] Образ HDD создаётся непосредственно в `Platform` и напрямую подключается к статическому `AtaDrive`.
- [x] Для `DiskImage` добавлен прямой setter файлового фильтра.
- [x] Соответствующие объявления и связи удалены из встроенного и справочного `vector.conf`.

### Итерация 1.33 — статический загрузчик и лента

- [x] `VectorFileLoader` создаётся непосредственно в `Platform`, связывается с основной RAM и получает прежний файловый фильтр.
- [x] Входной и выходной `TapeRedirector` создаются напрямую с прежними режимами и фильтрами.
- [x] Все три комплекта ловушек загрузки, сохранения и закрытия файлов создаются напрямую с прежними адресами и сигнатурами.
- [x] Ловушки непосредственно регистрируются в `Cpu8080`, без конфигурационного парсера.
- [x] `tapeGrp` создаётся напрямую и сохраняет прежний состав, поэтому переключатель tape hook в меню продолжает работать.
- [x] Для `TapeRedirector` и `RkTapeOutHook` добавлены прямые setters параметров, ранее доступных только через `setProperty()`.
- [x] Соответствующий раздел File I/O удалён из встроенного и справочного `vector.conf`.

### Итерация 1.34 — статические EDD и ожидания CPU

- [x] Стандартная память EDD объёмом 256 КиБ создаётся непосредственно в `Platform`.
- [x] EDD напрямую подключается к `VectorAddrSpace` как RAM-диск 0.
- [x] `VectorRamDiskSelector` создаётся напрямую и подключается к порту `0x10`.
- [x] `VectorCpuWaits` создаётся непосредственно и напрямую подключается к `Cpu8080`.
- [x] Удалены конфигурационные варианты `EDDx2`, `ERAM` и `Z80`; целевая сборка фиксирована как Вектор-06Ц 8080 с обычным EDD.
- [x] Аппаратная часть встроенного и справочного `vector.conf` сведена к параметрам платформы и WX-включению.

### Итерация 1.35 — удаление конфигурации платформы

- [x] `Platform` больше не запускает `ConfigReader` и не принимает имя конфигурационного файла.
- [x] `Emulation` создаёт единственную платформу прямым вызовом `new Platform("vector")`.
- [x] Удалён встроенный `src/pico/vector06c_config.h`.
- [x] Удалён ставший справочным `dist/vector/vector.conf`.
- [x] Статическая конфигурация Вектор-06Ц больше не зависит от конфигурационного парсера или файловой системы.

## Критерии готовности

-   [x] Работает без SD.
-   [x] Только Вектор-06Ц.
-   [x] Один видеорежим.
-   [x] Статическая конфигурация.
-   [ ] Полностью работает меню.
-   [ ] Работают snapshot.
-   [x] Все ROM встроены.
-   [x] Мультиплатформенность удалена.


### Итерация 1.36 — удаление динамического парсера и фабрики объектов

- [x] Удалены больше не используемые `ConfigReader.cpp/.h`.
- [x] Удалены больше не используемые `ObjectFactory.cpp/.h`.
- [x] Из `Emulation.cpp` удалено последнее подключение `ConfigReader.h`.
- [x] Pico-сборка больше не компилирует таблицу регистраций универсальных классов и код разбора конфигурационных файлов.
- [x] Статическое создание Вектор-06Ц остаётся единственным путём построения аппаратной модели.


### Итерация 1.37 — удаление мёртвого слоя EmuConfig

- [x] Удалены больше не используемые `EmuConfig.cpp/.h`.
- [x] `Emulation` больше не создаёт и не хранит объект конфигурации.
- [x] Удалены пустые обновления конфигурационного окна из `EmuWindow` и `Platform`.
- [x] Удалены поиск вкладки конфигурации и связанные RTTI-хуки `EmuConfigTab`/`EmuConfigControl`.
- [x] На этом этапе запросы `SR_CONFIG` и `SR_HELP` были оставлены пустыми; окончательно удалены после аудита источников в итерации 1.54.
- [x] Внешний `emuGetPlatforms()` временно сохранён как пустой совместимый API; удалён в итерации 1.40.

### Итерация 1.38 — фиксированный масштаб окна

- [x] Удалены `FrameScale`, `setFrameScale()` и `setFixedYScale()`.
- [x] Оконный вывод Вектора фиксирован на прежнем коэффициенте 2×; `Platform` больше не настраивает масштаб во время запуска.
- [x] Удалены свойства `frameScale` и все варианты 1×/1,5×/2×/2,5×/3×/4×/5×/fit.
- [x] Удалены системные запросы и сочетания Alt+0..5/8/9 для изменения масштаба.
- [x] Удалён режим максимизации Shift+Alt+M; Alt+M всегда управляет звуком.
- [x] Полноэкранный вывод сохранён и рассчитывается отдельно, без универсального переключателя масштаба.


### Итерация 1.39 — фиксированный оконный режим

- [x] Удалён `SR_FULLSCREEN` и обработка `Alt+Enter`.
- [x] Удалены `setFullScreen()`, `toggleFullScreen()` и состояние полноэкранного режима.
- [x] Удалено свойство `fullscreen` у `EmuWindow`.
- [x] Удалён внешний совместимый API `emuExitFullscreenMode()`.
- [x] Вывод окончательно зафиксирован в оконном режиме 800×600.


### Итерация 1.40 — удаление API выбора платформы

- [x] Удалён пустой внешний API `emuGetPlatforms()`.
- [x] Удалена больше не используемая структура `PlatformInfo`.
- [x] Удалена пустая PAL-заглушка `palChoosePlatform()`.
- [x] Удалены ставшие ненужными подключения `<vector>`.
- [x] В runtime-интерфейсе больше нет представления списка платформ или выбора другой машины.


### Итерация 1.41 — прямое владение единственной платформой

- [x] Удалена абстракция `ParentObject`, оставшаяся от динамического построения дерева объектов.
- [x] `Emulation` и `Platform` теперь непосредственно наследуются от `EmuObject`.
- [x] Удалён неиспользуемый `Emulation::addChild()` и динамическое назначение активной платформы через RTTI.
- [x] Единственная платформа по-прежнему создаётся и сохраняется напрямую в `Emulation::m_activePlatform`.
- [x] Удалены системные запросы и горячие клавиши смены платформы/конфигурации (`Alt+F8`, `Alt+F9`).
- [x] Удалены закомментированные циклы по старому списку платформ.


### Итерация 1.42 — фиксированная геометрия и фильтрация изображения

- [x] Удалены runtime-переключатели коррекции пропорций, широкого экрана и сглаживания.
- [x] Удалены горячие клавиши `Alt+R`, `Alt+N` и `Alt+S`.
- [x] Удалены свойства окна `aspectCorrection`, `wideScreen`, `customScreenFormat`, `squarePixels` и `smoothing`.
- [x] Удалены связанные поля и методы `EmuWindow`.
- [x] Размер области вывода теперь рассчитывается напрямую как удвоенный размер исходного кадра.
- [x] Фильтрация PAL остаётся жёстко заданной как `ST_SHARP` при создании окна.


### Итерация 1.43 — статическое окно 800×600

- [x] Удалены универсальные режимы окна `WS_AUTOSIZE`, `WS_FIXED`, `WS_RESIZABLE`.
- [x] Удалены режимы смешивания полукадров и неиспользуемые буферы interlace/scanline.
- [x] Удалены свойства `windowStyle`, `fieldsMixing`, `defaultWindowSize`, `defaultWindowWidth`, `defaultWindowHeight`.
- [x] Окно создаётся сразу как фиксированное 800×600; платформа больше не настраивает его геометрию.
- [x] Удалён большой закомментированный desktop-путь отрисовки из `EmuWindow::drawFrame()`.
- [ ] Собственное меню и прямой видеовывод Pico остаются следующими этапами.

### Итерация 1.44 — исключение остальных платформ из сборки

- [x] Pico-сборка больше не компилирует реализации 15 других машин Emu80.
- [x] Из списка исходников исключены `Apogey`, `Bashkiria`, `Eureka`, `Korvet`, `КР-04`, `Львов`, `Микро-80`, `Микроша`, `Орион`, `Пальмира`, `Партнёр`, `ПК8000`, `РК86`, `Специалист` и `ЮТ-88`.
- [x] Общие устройства и базовые классы сохранены, поэтому статическая модель Вектора продолжает использовать их без дублирования.
- [x] Перечень исключений задан централизованно в `CMakeLists.txt` сразу после исходного `GLOB`.
- [x] Исходники чужих платформ физически остаются в дереве до отдельной итерации удаления файлов, но больше не входят в Pico-прошивку.



### Итерация 1.45 — физическое удаление остальных платформ

- [x] Из дерева исходников удалены реализации и заголовки 15 чужих машин, ранее исключённых из Pico-сборки.
- [x] Удалены `Apogey`, `Bashkiria`, `Eureka`, `Korvet`, `КР-04`, `Львов`, `Микро-80`, `Микроша`, `Орион`, `Пальмира`, `Партнёр`, `ПК8000`, `РК86`, `Специалист` и `ЮТ-88`.
- [x] Из `CMakeLists.txt` удалён временный список `UNUSED_PLATFORM_SOURCES`: отсутствующие файлы больше не попадают в исходный `GLOB`.
- [x] Общие устройства и базовые классы сохранены; удалены только конечные реализации чужих машин.
- [x] Пункт этапа 1 «Удалить остальные платформы» закрыт.

### Итерация 1.46 — удаление встроенных ROM и шрифтов чужих платформ

- [x] Из `Memory.cpp` удалены таблица встроенных ROM остальных машин и ветви выбора по их файловым именам.
- [x] В прошивке оставлен единственный встроенный ROM `vector/loader.rom`; для остальных имён сохранён общий файловый путь `palReadFromFile()`.
- [x] Из `CrtRenderer.cpp` удалены встроенные шрифты RK86, Апогея, Партнёра и Пальмиры вместе со специальными ветвями выбора.
- [x] Физически удалены 34 сгенерированных заголовка с ROM, шрифтами и служебными образами уже удалённых платформ.
- [x] Размер исходного дерева и объём данных во flash больше не включают ресурсы чужих машин.


### Итерация 1.47 — удаление неиспользуемых общих модулей

- [x] После удаления остальных платформ из дерева убраны общие модули, которые больше не создаются статической моделью Вектора.
- [x] Удалены контроллеры и адаптеры `Pic8259`, `PpiAtaAdapter`, `RkFdd`, `RkRomDisk`, `RkSdController`, `SdCard` и `SdAdapters`.
- [x] Удалены неиспользуемые клавиатурные классы RK-семейства `RkKeyboard` и `RkPpi8255Circuit`.
- [x] Удалены неиспользуемые наборы перехватчиков `MsxTapeHooks` и `RfsTapeHooks`, а также пустой остаточный модуль `GenericModules`.
- [x] Сохранены `RkTapeHooks`, поскольку они непосредственно используются загрузкой и сохранением ленты Вектора.
- [x] Благодаря исходному `GLOB` удалённые файлы автоматически исключены из Pico-сборки без дополнительных списков в CMake.


### Итерация 1.48 — аудит накопителей и восстановление EDD2

- [x] Отбракован прежний патч 1.48; его архитектурные удаления не используются.
- [x] Построена карта зависимостей FDD, HDD, EDD и EDD2 по исходному emu80v4 и текущей статической модели.
- [x] Подтверждено, что HDD Вектора использует отдельную цепочку `DiskImage -> AtaDrive -> VectorHddRegisters` и не связан с дисководами C/D.
- [x] Обнаружена ошибка итерации 1.34: не были перенесены файловая оболочка первого EDD и вся цепочка EDD2.
- [x] Восстановлен `RamDisk` для EDD, обеспечивающий загрузку и сохранение `.edd`.
- [x] Восстановлены память EDD2 объёмом 256 КиБ, селектор порта `0x11` и файловая оболочка `RamDisk` с меткой `EDD2`.
- [x] Проверено, что удаления 1.44–1.47 не затронули модули, создаваемые исходной конфигурацией Вектора.
- [x] Добавлен документ `vector06c_storage_dependency_map.md`; дальнейшие удаления должны проверяться по исходной конфигурации, статическому созданию и runtime-вызовам.


### Итерация 1.49 — удаление отсутствующих дисководов C и D

- [x] По карте зависимостей подтверждено, что исходная конфигурация Вектора создаёт только FDD A и B.
- [x] Подтверждено, что HDD использует отдельные `DiskImage("HDD")`, `AtaDrive` и `VectorHddRegisters`, а EDD/EDD2 используют `RamDisk` и `SRam`.
- [x] Удалены системные запросы `SR_DISKC` и `SR_DISKD`.
- [x] Удалены поля `Platform::m_diskC` и `Platform::m_diskD`, а также поиск меток `C` и `D`.
- [x] Удалены недействующие сочетания `Shift+Alt+C` и `Shift+Alt+D`; обычные `Alt+C` и `Alt+D` продолжают включать цвет и отладчик.
- [x] Цепочки FDD A/B, HDD, EDD и EDD2 не изменены.


### Итерация 1.50 — прямое закрепление объектов платформы

- [x] После статического создания Вектора ссылки на окно, CPU, ядро, рендерер, клавиатуру, раскладку и `KbdTapper` закрепляются непосредственно.
- [x] Ссылки на FDD A/B, HDD, EDD и EDD2 закрепляются при создании соответствующих объектов; состав и подключение накопителей не изменены.
- [x] Группа кассетных перехватчиков и загрузчик также закрепляются напрямую.
- [x] Удалены повторные обходы `m_objList`, которые заново определяли уже известные роли через виртуальные методы `as...()`.
- [x] Виртуальные методы `as...()` пока сохранены; их удаление вынесено в отдельный аудит пользователей интерфейса.
- [x] Порядок создания, `init()`, `reset()` и показа окна не изменён.


### Итерация 1.51 — удаление неиспользуемого интерфейса определения ролей

- [x] После прямого закрепления объектов проверены все оставшиеся вызовы методов `as...()` базового `EmuObject`.
- [x] Удалены только методы с нулём реальных вызовов: `asPlatform`, `asEmuWindow`, `asCpu`, `asPlatformCore`, `asKbdLayout`, `asCrtRenderer`, `asDiskImage`, `asFileLoader`, `asRamDisk`, `asKeyboard`, `asEmuObjectGroup`, `asKbdTapper`.
- [x] Сохранены используемые интерфейсы `asCpu8080Compatible`, `asCpuZ80`, `asRam`, `asSRam` и отдельный оконный `asDebugWindow`.
- [x] Создание, подключение и порядок инициализации FDD A/B, HDD, EDD/EDD2 и кассетных устройств не изменены.


### Итерация 1.52 — удаление неиспользуемых свойств платформы и fast reset

- [x] По исходной и текущей конфигурациям проверены свойства `helpFile`, `codePage`, `muteTape`, `fastReset` и `fastResetCpuTicks`; ни одно из них не задаётся и не читается Вектором.
- [x] Удалены остаточные обработчики свойств `Platform`, их поля и неиспользуемый тип `CodePage`.
- [x] Удалены системный запрос `SR_FASTRESET`, сочетание `Alt+U` и недостижимая ветвь ускоренного reset; обычный `SR_RESET` не изменён.
- [x] Из остаточного desktop-конфигуратора удалён пункт выбора таблицы символов, который ссылался на удалённое свойство `codePage`.
- [x] FDD A/B, HDD, EDD/EDD2, кассета, загрузчик и порядок инициализации не изменены.


### Исправление 1.52.1 — завершение удаления свойств Platform

- [x] Удалены оставшиеся override-объявления `Platform::setProperty()` и `Platform::getPropertyStringValue()`, которые после 1.52 создавали отсутствующие записи vtable.
- [x] Сборка и запуск подтверждены на целевой прошивке.

### Итерация 1.53 — удаление остаточного desktop-debug слоя Platform

- [x] Подтверждено, что `Platform::showDebugger()` и `Platform::updateDebugger()` пусты, а действующий `SR_DEBUG` напрямую вызывает `g_emulation->debugRequest(m_cpu)`.
- [x] Удалены неиспользуемые методы `showDebugger`, `updateDebugger`, `reqScreenUpdateForDebug`, `getAllDebugInfo` и `updateScreenOnce`, а также их пустые вызовы из `FileLoader` и `Platform`.
- [x] Удалены никогда не назначаемый второй рендерер `m_renderer2` и неиспользуемые поля/геттеры `m_baseDir`, `m_baseName`.
- [x] Основной рендерер, загрузчик, отладочный запрос, FDD A/B, HDD, EDD/EDD2, кассета и порядок инициализации не изменены.


### Итерация 1.54 — удаление недостижимых системных запросов

- [x] Проверены все значения `SysReq` по источникам создания, обработчикам и текущим сочетаниям клавиш.
- [x] Удалены значения `SR_MENU` и `SR_ABOUT`, которые существовали только в enum и нигде не создавались.
- [x] Удалены пустые `SR_HELP` и `SR_CONFIG` вместе с недействующими сочетаниями `Alt+F1` и `Alt+F12`.
- [x] Удалены недостижимые раздельные команды pause on/off и printer capture on/off; действующие переключатели `SR_PAUSE` и `SR_PRNCAPTURE` сохранены.
- [x] Удалены недостижимые прямые команды load/save для EDD и EDD2; доступные команды `Open` и `Save As` для обоих RAM-дисков сохранены.
- [x] Reset, загрузка файлов, FDD A/B, HDD, EDD/EDD2, кассета, звук, отладчик и управление скоростью не изменены.


### Итерация 1.55 — удаление группового и внешнего интерфейса свойств

- [x] Удалён `EmuObjectGroup`: после статической сборки Вектора он использовался только для группового переключения магнитофонных CPU hooks.
- [x] Магнитофонные hooks хранятся в `Platform` как типизированный список `CpuHook*` и переключаются прямыми `getEnabled()`/`setEnabled()`.
- [x] Удалены внешние PAL-функции `emuSetPropertyValue()` и `emuGetPropertyValue()`.
- [x] Загрузка `.fdd` больше не ищет `diskA` по строковому имени и не задаёт `fileName` через универсальное свойство; используется прямой `Platform::assignDiskAFileName()`.
- [x] Состав FDD A/B, HDD, EDD и EDD2 не изменён.


### Итерация 1.56 — удаление фабрик динамической конфигурации

- [x] После удаления `ConfigReader` проверены все `static create(const EmuValuesList&)`; реальных вызовов этих фабрик в специализированном дереве нет.
- [x] Удалены 54 неиспользуемых фабричных методов из классов устройств и компонентов Вектора.
- [x] Удалён ставший ненужным `EmuObject::findObj()`, использовавшийся только удалёнными фабриками.
- [x] Рабочие `setProperty()` и реестр объектов пока сохранены: их удаление требует отдельного аудита оставшихся строковых связей.
- [x] Создание устройств в `Platform`, порядок `init()`/`reset()`, FDD A/B, HDD, EDD/EDD2 и кассетные hooks не изменены.

### Итерация 1.57 — удаление строковой системы свойств

- [x] Проверены все внешние вызовы `setProperty()` и `getPropertyStringValue()`; после удаления динамической конфигурации и PAL-мостов реальных потребителей не осталось.
- [x] Удалены базовый виртуальный интерфейс свойств `EmuObject` и 63 остаточные реализации в компонентах эмулятора и Вектора. Вместе с ними удалены ставшие полностью неиспользуемыми `EmuValue`, `EmuValuesList` и файлы `Parameters.*`.
- [x] Удалены строковые привязки объектов, параметры старых конфигураций и обратное преобразование значений, выполнявшиеся только внутри недостижимых обработчиков свойств.
- [x] `Platform::m_objList` и `addChild()` сохранены: список по-прежнему управляет порядком `init()`, `reset()`, `shutdown()` и уничтожением объектов.
- [x] Глобальный реестр имён пока сохранён только для отдельного аудита жизненного цикла; FDD A/B, HDD, EDD/EDD2 и кассетные устройства не изменены.
### Итерация 1.58 — сокращение базового виртуального интерфейса

- [x] Проверены все виртуальные методы `EmuObject` и их реальные вызовы после удаления desktop-debug и строковой конфигурации.
- [x] Удалён неиспользуемый `EmuObject::getDebugInfo()` и остаточные реализации в `Crt8275`, `Dma8257`, `Fdc1793`, `VectorAddrSpace` и `VectorRenderer`.
- [x] Удалены ставшие ненужными подключения `<sstream>`/`<iomanip>` в соответствующих модулях.
- [x] `setPlatform()` сделан невиртуальным: переопределений у него нет, а рабочие вызовы из `Platform` сохранены.
- [x] Рабочие виртуальные методы `setFrequency()`, `init()`, `shutdown()`, `reset()`, `notify()`, а также `asRam()`/`asSRam()` сохранены.
- [x] Порядок жизненного цикла объектов, FDD A/B, HDD, EDD/EDD2 и кассетные hooks не изменены.
- [x] Удалены старые закомментированные реализации `getDebugInfo()` в CPU и PIT и ставшие ненужными подключения форматирования строк.
- [x] `setPlatform()` сделан невиртуальным: переопределений этого метода нет, все рабочие вызовы сохранены.
- [x] Рабочие `init()`, `reset()`, `shutdown()`, `notify()`, `setFrequency()`, `asRam()` и `asSRam()` не изменены.


### Итерация 1.59 — аудит остаточного RTTI-подобного интерфейса

- [x] Проверены все оставшиеся методы вида `as...()` и их реальные вызовы в специализированном дереве.
- [x] Удалены неиспользуемые `Cpu8080Compatible::asCpuZ80()` и `CpuZ80::asCpuZ80()`; определение типа CPU продолжает выполняться рабочим `getType()`.
- [x] Удалён неиспользуемый `PalWindow::asDebugWindow()` и ставшее лишним предварительное объявление `DebugWindow`.
- [x] Сохранён `Cpu::asCpu8080Compatible()`: он используется загрузчиком программ до и после reset.
- [x] Сохранены `AddressableDevice::asRam()` и `asSRam()`: они используются `RamDisk` для определения фактического размера страниц EDD/EDD2.
- [x] Создание CPU, загрузчик, FDD A/B, HDD, EDD/EDD2 и порядок жизненного цикла объектов не изменены.

### Итерация 1.60 — удаление поиска объектов по имени

- [x] Проверены все пользователи глобального списка `Emulation::m_objectList`: список сохраняется как действующий механизм регистрации и аварийного уничтожения объектов вне `Platform`.
- [x] Удалён `Emulation::findObject()`: после удаления конфигурационного парсера, фабрик и строковых свойств поиск объектов по имени больше нигде не используется.
- [x] Удалена генерация имён платформы с суффиксами `$1`, `$2` и далее; специализированная прошивка создаёт ровно один `Platform("vector")`.
- [x] Имена объектов пока сохранены: пустое имя отличает динамические breakpoint hooks в `Cpu`, а имя платформы возвращает оконный API.
- [x] Порядок регистрации, уничтожения, `init()`/`reset()`/`shutdown()`, FDD A/B, HDD, EDD/EDD2 и кассетные hooks не изменены.


### Итерация 1.61 — удаление глобального реестра объектов

- [x] Проверены все создания `EmuObject` вне `Platform`: `SoundMixer`, `WavReader`, `PrnWriter`, вложенный `WavSoundSource`, `WavWriter`, `CloseFileTimer` и счётчики PIT имеют явных владельцев или удаляются в соответствующих классах.
- [x] `PrnWriter`, ранее удалявшийся только аварийным обходом глобального списка, теперь явно удаляется в `Emulation::~Emulation()`.
- [x] Удалены `Emulation::m_objectList`, `addObject()`/`removeObject()` и автоматическая регистрация из конструктора/деструктора каждого `EmuObject`.
- [x] `Platform::m_objList` сохранён без изменений: он по-прежнему задаёт порядок `init()`, `reset()`, `shutdown()` и уничтожения аппаратных объектов Вектора.
- [x] Имена объектов, CPU hooks, FDD A/B, HDD, EDD/EDD2, кассета и рабочая логика эмуляции не изменены.


### Исправление 1.61.1 — явное подключение std::list

- [x] В `Platform.h` добавлен прямой `#include <list>` для рабочего `Platform::m_objList`.
- [x] Устранена скрытая зависимость от транзитивного подключения `<list>` через удалённый глобальный реестр объектов.
- [x] Жизненный цикл аппаратных объектов и логика эмуляции не изменены.

### Итерация 1.62 — удаление строковых имён объектов

- [x] После удаления конфигурации, поиска и глобального реестра проверены все оставшиеся вызовы `setName()`/`getName()`.
- [x] Из `EmuObject` удалены поле `m_name` и методы управления строковым именем; удалены ставшие бессмысленными назначения имён статически создаваемым объектам.
- [x] Имя единственной платформы для PAL возвращается напрямую как `vector`; конструктор `Platform` больше не принимает строковое имя.
- [x] Удалена старая ветвь уничтожения безымянных breakpoint hooks: подсистема desktop-debug удалена, а все существующие CPU hooks явно принадлежат `Platform`.
- [x] `Platform::m_objList`, порядок `init()`/`reset()`/`shutdown()` и уничтожение аппаратных объектов не изменены.

### Исправление 1.62.1 — явное подключение std::string

- [x] В `CpuHook.h` добавлен прямой `#include <string>` для публичной сигнатуры `setSignature(std::string)`.
- [x] Устранена скрытая зависимость от транзитивного подключения `<string>` через удалённое поле имени `EmuObject`.
- [x] CPU hooks, кассетные ловушки и логика эмуляции не изменены.

### Итерация 1.63 — явные зависимости заголовков

- [x] Проведён include-аудит заголовков, затронутых удалением глобального реестра и строковых имён объектов.
- [x] `Platform.h` теперь явно подключает используемые `<list>`, `<string>` и `<vector>`; неиспользуемый `<map>` удалён.
- [x] `CpuHook.h` явно подключает `<cstdint>` для `uint8_t`/`uint16_t`, а `Emulation.h` — `<cstdint>`, `<string>` и `<vector>` для собственного публичного интерфейса и полей.
- [x] Из `EmuObjects.h` удалён неиспользуемый `<vector>`, который ранее случайно скрывал зависимости других заголовков.
- [x] Исполняемый код, порядок жизненного цикла, CPU hooks, FDD A/B, HDD и EDD/EDD2 не изменены.


### Исправление 1.63.1 — явное подключение std::vector в Cpu.h

- [x] В `Cpu.h` добавлен прямой `#include <vector>` для рабочего `std::vector<CpuHook*> m_hookVector`.
- [x] Устранена скрытая зависимость от ранее транзитивного подключения `<vector>` через `EmuObjects.h`.
- [x] Логика CPU hooks, порядок их регистрации и выполнение эмулятора не изменены.


### Итерация 1.64 — удаление неработающего контура запросов перерисовки

- [x] Проверены все пользователи `Emulation::screenUpdateReq()`, `draw()` и ограничения FPS.
- [x] Подтверждено, что единственный код обработки `m_scrUpdateReq` и `m_fpsLimit` много итераций находился внутри полностью закомментированного блока `mainLoopCycle()`.
- [x] Удалены неиспользуемые методы `screenUpdateReq()`, `draw()`, `setFrameRate()`, поля состояния запроса перерисовки и старый закомментированный планировщик кадров.
- [x] Из `VectorCore::vrtc()` удалена установка флага, который больше нигде не считывался; генерация кадрового прерывания Вектора не изменена.
- [x] Фактический видеовывод, VSync, CPU timing, FDD A/B, HDD, EDD/EDD2 и кассетные hooks не изменены.


### Итерация 1.65 — удаление остаточного виртуального интерфейса draw

- [x] После удаления неработающего планировщика кадров повторно проверены все объявления и вызовы `draw()` в `Platform`, `PlatformCore` и `VectorCore`.
- [x] Подтверждено, что `Platform::draw()` больше нигде не вызывается, а `VectorCore::draw()` содержит только давно закомментированный desktop-рендеринг.
- [x] Удалены `Platform::draw()`, виртуальный `PlatformCore::draw()` и пустое переопределение `VectorCore::draw()`.
- [x] Фактический видеовывод Murmulator, VSync, кадровые прерывания, CPU timing, FDD A/B, HDD, EDD/EDD2 и кассетные hooks не изменены.


### Итерация 1.66 — удаление неиспользуемого сигнала HRTC

- [x] Проверены все объявления, переопределения и вызовы `PlatformCore::hrtc()`.
- [x] Подтверждено, что после специализации на Вектор-06Ц метод существует только как пустой виртуальный stub и нигде не вызывается.
- [x] Удалён неиспользуемый виртуальный метод `PlatformCore::hrtc()`.
- [x] Рабочие сигналы VRTC/INTE, таймеры, кассетный выход, видеовывод, FDD A/B, HDD и EDD/EDD2 не изменены.


### Итерация 1.67 — упрощение остаточного PlatformCore

- [x] Проведён совместный аудит остаточного интерфейса `PlatformCore` и связанного с ним кода периферийных устройств.
- [x] Удалены неиспользуемые объявления `attachKeyboard()` и `attachKbdLayout()`, для которых в специализированном дереве не существовало ни реализаций, ни вызовов.
- [x] Удалён неработающий мост PIT → `PlatformCore::timer()`: счётчики PIT никогда не получали указатель на core, поэтому ветвь уведомления была недостижима; вместе с ней удалены ставшие пустыми `outChangeNotify()` и состояние `m_prevOut`.
- [x] Тривиальные конструктор, деструктор и `attachWindow()` перенесены в заголовок; пустой `PlatformCore.cpp` удалён.
- [x] Рабочие сигналы VRTC/INTE, кассетный выход, звук PIT, видеовывод, FDD A/B, HDD и EDD/EDD2 не изменены.

### Итерация 1.68 — удаление абстракции PlatformCore

- [x] Проведён полный аудит оставшегося `PlatformCore`: после специализации прошивки все рабочие пользователи фактически обслуживают только `VectorCore`.
- [x] Класс `PlatformCore` и его заголовок удалены; `VectorCore` теперь напрямую наследуется от `EmuObject`.
- [x] CPU и `WavWriter` переведены с абстрактного `PlatformCore*` на явный `VectorCore*`; состояние кассетного выхода перенесено в `VectorCore`.
- [x] Удалены неиспользуемые окно и renderer-связь ядра, а также пустой конструктор `VectorCore`.
- [x] Из неиспользуемого в Vector-конфигурации `Crt8275` удалена остаточная связь с платформенным ядром и вызовы VRTC, для которых больше не существует подключаемого core.
- [x] Рабочие VRTC/INTE Вектора, запись кассеты, видеовывод, CPU timing, FDD A/B, HDD и EDD/EDD2 сохранены.

### Итерация 1.69 — удаление класса Platform

- [x] Единственный контейнер машины `Platform` объединён с `VectorCore`; отдельные `Platform.h` и `Platform.cpp` удалены.
- [x] `Emulation` теперь напрямую создаёт, хранит и уничтожает `VectorCore`.
- [x] В `VectorCore` перенесены статическое создание устройств, список владения, порядок `init()`/`reset()`/`shutdown()`, обработка системных запросов, клавиатуры, мыши и загрузки файлов.
- [x] Устройства переведены с `Platform*` и `m_platform` на явную связь с единственной машиной `VectorCore*`; методы владения переименованы в `setMachine()`/`getMachine()`.
- [x] CPU подключается непосредственно к владеющему `VectorCore`, а `WavWriter` получает этот же объект без промежуточного `getCore()`.
- [x] Состав и порядок дочерних устройств, VRTC/INTE, кассета, видеовывод, FDD A/B, HDD, EDD/EDD2 и обработка ввода сохранены.


### Итерация 1.70 — удаление остатков выбора платформы

- [x] Термин `activePlatform` в рабочем пути клавиатуры заменён на прямое имя единственной машины; обработка клавиш и системных сочетаний не изменена.
- [x] Из `PalWindow` и `EmuWindow` удалён неиспользуемый виртуальный `getPlatformObjectName()`, возвращавший константу `vector`.
- [x] Из Pico PAL удалены неиспользуемые API и пустые заглушки выбора платформы, platform defines и desktop-окна конфигурации.
- [x] Общий Platform Abstraction Layer (`Pal*`) сохранён: удалена только инфраструктура выбора эмулируемой платформы.
- [x] Создание VectorCore, ввод, VRTC/INTE, кассета, звук, FDD A/B, HDD и EDD/EDD2 не изменены.

### Итерация 1.71 — RAII-владение устройствами VectorCore

- [x] Проверено назначение прежнего `m_objList`: он не образовывал рабочую иерархию, а служил только реестром жизненного цикла и ручного владения устройствами.
- [x] Ручная схема `new` → `addChild()` → `delete` заменена единым шаблонным `addDevice<T>()` и реестром `std::unique_ptr<EmuObject>`.
- [x] Все 52 устройства получают `VectorCore` при создании; отдельный `addChild()` и список сырых владеющих указателей удалены.
- [x] Порядок создания, `init()`, `reset()`, `shutdown()` и уничтожения устройств сохранён.
- [x] Рабочие связи CPU, памяти, видео, клавиатуры, кассеты, звука, FDD A/B, HDD и EDD/EDD2 не изменены.


### Итерация 1.72 — RAII-владение корневыми объектами эмуляции

- [x] `Emulation` и `VectorCore` больше не наследуются от `EmuObject`: они являются владельцами и координаторами, а не эмулируемыми устройствами.
- [x] Владение `SoundMixer`, `WavReader`, `PrnWriter` и единственным `VectorCore` переведено с ручных `new`/`delete` на `std::unique_ptr`.
- [x] Порядок уничтожения сохранён: сначала машина, затем принтер, WAV-reader и в последнюю очередь sound mixer.
- [x] Закрытие машины и ошибка создания окна используют `reset()` владельца без ручного удаления и обнуления указателя.
- [x] Частоты, активные устройства, ввод, звук, кассета, FDD A/B, HDD и EDD/EDD2 не изменены.


### Итерация 1.73 — минимизация базового EmuObject

- [x] Проведён полный аудит интерфейса `EmuObject` и его фактических пользователей после удаления `Platform`/`PlatformCore` и перехода на RAII.
- [x] Удалён неиспользуемый `getMachine()`: все устройства используют защищённую ссылку `m_machine` напрямую, внешних пользователей accessor не осталось.
- [x] Универсальный виртуальный `EmuObject::notify()` удалён: единственная рабочая связь `DiskImage` → `AtaDrive` переведена на узкий типизированный интерфейс `DiskImageObserver`.
- [x] Событие открытия/закрытия образа теперь передаётся как `diskImageChanged(DiskImage*, bool)`, без числовых кодов и без зависимости всех устройств от чужого механизма уведомлений.
- [x] Деструктор `AddressableDevice` упрощён до `override = default`; удалены устаревшие комментарии и пустая секция класса.
- [x] Порядок `init()`/`reset()`/`shutdown()`, частоты, машинные связи, FDD A/B, HDD и EDD/EDD2 не изменены.


### Итерация 1.74 — удаление неиспользуемых memory tags

- [x] Проведён аудит пути тегов памяти: в текущей Vector-06Ц конфигурации теги нигде не назначаются, а обе реализации ожиданий процессора игнорируют параметр `memTag`.
- [x] Из `AddressableDevice` удалены `readByteEx()`/`writeByteEx()`, глобальный `m_lastTag`, неиспользуемые `m_supportsTags` и `m_tag`.
- [x] CPU выполняет обращения к памяти напрямую через `readByte()`/`writeByte()`; неиспользуемый `CpuCycleWaits` и всегда нулевой указатель `m_cycleWaits` удалены.
- [x] Интерфейс `CpuWaits` сокращён до реально используемых `opcode` и `normalClocks`; формулы ожиданий 8080 и Z80 не изменены.
- [x] Адресное пространство, маскирование адресов, команды CPU, VRTC/INTE, FDD A/B, HDD и EDD/EDD2 не изменены.


### Итерация 1.75 — удаление неиспользуемых вариантов адресного пространства

- [x] Проведён аудит всех классов `AddrSpace`: в специализированной конфигурации используются только основной диапазонный `AddrSpace` и `AddrSpaceInverter`.
- [x] Удалены неиспользуемые `AddrSpaceMapper`, `AddrSpaceShifter` и `AddrSpaceWriteSplitter`, не имевшие ни одного создания или вызова.
- [x] `addReadRange()` и `addWriteRange()` сделаны невиртуальными: наследников и переопределений у `AddrSpace` нет.
- [x] Из `AddrSpace.cpp` удалены неиспользуемые зависимости от `Globals.h` и `Emulation.h`, а также старые закомментированные массивы.
- [x] Рабочие диапазоны портов, инверторы адресов PPI/PIT/FDC, память, CPU, FDD A/B, HDD и EDD/EDD2 не изменены.


### Итерация 1.76 — типизация CPU единственной машины

- [x] `VectorCore` хранит и возвращает `Cpu8080Compatible*`: оба поддерживаемых процессора Вектора (8080 и Z80) принадлежат этой иерархии.
- [x] Из `Cpu` удалены неиспользуемые `CpuType`/`getType()` и ручной pseudo-RTTI `asCpu8080Compatible()`.
- [x] `FileLoader` и код Vector переведены на прямой типизированный доступ без проверок и `static_cast`, которые всегда завершались успешно.
- [x] Выбор 8080/Z80, реализация команд, hooks, wait states, память, FDD A/B, HDD и EDD/EDD2 не изменены.

### Итерация 1.77 — удаление неиспользуемого DMA/8275-тракта

- [x] Проведён аудит контроллера DMA 8257 и видеотракта 8275: в единственной машине Vector-06Ц эти устройства нигде не создаются и не подключаются.
- [x] Удалены `Dma8257`, `Crt8275`, `Crt8275Renderer` и обслуживавший только этот renderer класс `TextCrtRenderer`.
- [x] Из `Fdc1793` удалены необязательная связь с DMA и ветви обмена, достижимые только при ненулевом `m_dma`; программный обмен ерез регистр данных сохранён без изменений.
- [x] Удалены остаточные объявления, include и устаревший комментарий; glob-сборка больше не компилирует 1 800+ строк недостижимого egacy-кода.
- [x] Команды FDC, статусы без DMA, образы FDD A/B, HDD, EDD/EDD2, CPU, видео Вектора и ввод не изменены.


### Итерация 1.78 — удаление неиспользуемых дизассемблеров CPU

- [x] Проверено отсутствие вызовов функций дизассемблирования 8080 и Z80.
- [x] Удалены `Cpu8080dasm.cpp/.h`.
- [x] Удалены `CpuZ80dasm.cpp/.h`.
- [x] Исполняющие ядра `Cpu8080` и `CpuZ80` не изменены.
- [x] Удалены раскладка `KrKbdLayout` и звуковой адаптер `RkPit8253SoundSource`, относящиеся к отсутствующим конфигурациям.
- [x] `VectorEramSelector` и ERAM-ветви `VectorAddrSpace` сохранены: оригинальный `vector.conf` поддерживает отдельный режим `CFG_EDD=ERAM`.
- [x] Действующие `VectorFileLoader`, `VectorKbdLayout`, EDD/EDD2, PIT sound, CPU, память, FDD A/B и HDD не изменены.


### Итерация 1.80 — типизация CPU hooks

- [x] Подтверждено, что все оставшиеся CPU hooks используются только процессорами иерархии `Cpu8080Compatible` (8080 и Z80).
- [x] `CpuHook` теперь хранит `Cpu8080Compatible*`; удалены всегда успешные `static_cast` из базовой ловушки возврата и магнитофонных hooks.
- [x] Регистрация hooks перенесена из общего `Cpu` в `Cpu8080Compatible`; общий CPU больше не содержит неиспользуемый список hooks и виртуальные `addHook()`/`removeHook()`.
- [x] Удалён неиспользуемый счётчик `m_nHooks`; отключение hooks и адресная таблица ловушек сохранены.
- [x] Деструктор `Cpu8080Compatible` явно отсоединяет hooks и освобождает списки адресной таблицы; выполнение 8080/Z80, сигнатуры и кассетные ловушки не изменены.


### Итерация 1.81 — типизация страниц RAM-диска и удаление неиспользуемой маски адреса

- [x] Подтверждено, что оба оставшихся `RamDisk` используются только как файловые оболочки памяти `SRam` для EDD и EDD2.
- [x] Страницы `RamDisk` типизированы как `SRam*`; удалены последние pseudo-RTTI методы `asRam()`/`asSRam()` и проверки типов при загрузке и сохранении `.edd`.
- [x] Удалены `AddressableDevice::setAddrMask()` и `m_addrMask`: в статической конфигурации Вектора маска нигде не устанавливалась и всегда оставалась нулевой.
- [x] Из `Ram`, `Rom` и `AddrSpace` удалены недостижимые ветви маскирования адреса; рабочая проверка размера памяти и маршрутизация диапазонов сохранены.
- [x] Формат EDD/EDD2, размеры 256 КиБ, чтение/запись `.edd`, CPU, основная память, порты и накопители не изменены.

### Итерация 1.82 — уплощение загрузчика файлов Вектора

- [x] Подтверждено, что после удаления остальных платформ абстрактный `FileLoader` используется только единственным `VectorFileLoader`.
- [x] Общая реализация выбора файла перенесена непосредственно в `VectorFileLoader`; загрузчик больше не наследуется от `FileLoader`.
- [x] `VectorCore` хранит типизированный `VectorFileLoader*`, а загрузчик напрямую содержит своё адресное пространство, фильтр и задержку запуска.
- [x] Из рабочего пути удалены неиспользуемые поля общей инфраструктуры: tape redirector, last file и параметры multiblock.
- [x] Старые `FileLoader.cpp/.h` пока оставлены как неиспользуемые совместимые единицы исходников: это делает патч независимым от различий BOM/CRLF в пользовательском дереве; к рабочему графу объектов они больше не подключены.
- [x] Загрузка `.rom`, `.r0m`, `.vec`, `.cas`, `.bas` и `.fdd`, режим Run, CPU hooks, память и накопители не изменены.


### Итерация 1.83 — удаление отключённого FileLoader

- [x] Завершено уплощение загрузчика файлов: старые `FileLoader.cpp/.h`, не входившие после 1.82 в рабочий граф объектов, удалены из дерева.
- [x] Из `EmuObjects.h` удалено остаточное предварительное объявление `FileLoader`.
- [x] Единственным загрузчиком остаётся типизированный `VectorFileLoader`; выбор и загрузка файлов выполняются непосредственно им.
- [x] Патч удаления сформирован непосредственно от присланного подтверждённого ZIP, поэтому контекст удаления совпадает с пользовательским деревом.
- [x] Форматы `.rom`, `.r0m`, `.vec`, `.cas`, `.bas`, `.fdd`, режим Run, память, CPU и накопители не изменены.

### Итерация 1.84 — явные типы памяти VectorAddrSpace

-   [x] Основная память `VectorAddrSpace` типизирована как `Ram*` вместо общего `AddressableDevice*`.
-   [x] Обе области памяти RAM-дисков типизированы как `SRam*`.
-   [x] `VectorFileLoader` теперь принимает и хранит `Ram*`, поскольку загрузчик всегда пишет только в основную RAM Вектора.
-   [x] Удалены лишние универсальные типы на путях, где конфигурация Вектора уже статична.


### Итерация 1.85 — сокращение внешнего интерфейса VectorCore

- [x] Проведён аудит открытых accessor-ов `VectorCore`: `getLoader()`, `getKbdLayout()` и `getRenderer()` не имеют ни одного внешнего пользователя.
- [x] Неиспользуемые accessor-ы удалены; соответствующие устройства остаются внутренними связями единственной машины.
- [x] Рабочие `getWindow()`, `getCpu()` и `getKeyboard()` сохранены, поскольку используются устройствами и координатором эмуляции.
- [x] Загрузка файлов, раскладка клавиатуры, renderer, системные команды, CPU, память и накопители не изменены.

## Итерация 1.86 — локализовать доступ к адресному пространству при загрузке FDD

- [x] В ветке загрузки `.fdd` получить `VectorAddrSpace` один раз через локальную переменную.
- [x] Убрать повторные цепочки `m_machine->getCpu()->getAddrSpace()`.
- [x] Не менять порядок сброса, запуска CPU и переключения ROM.

## Итерация 1.87 — локализовать доступ к CPU в обработчике F11/F12

- [x] CPU и `VectorAddrSpace` получаются один раз в начале `processSpecialKeys()`.
- [x] Убраны повторные цепочки `m_machine->getCpu()->getAddrSpace()`.
- [x] Поведение F11/F12, reset, ROM и PC не изменено.


## Итерация 1.88 — локализовать доступ к клавиатуре при F12

- [x] В ветке F12 объект клавиатуры получается один раз через локальную переменную.
- [x] Убраны повторные вызовы `m_machine->getKeyboard()` вокруг reset.
- [x] Порядок блокировки сброса клавиш, reset, восстановления и отключения ROM не изменён.

## Итерация 1.89 — исправить аудит поддержки ERAM

- [x] Сверены текущие исходники с оригинальным `dist/vector/vector.conf`.
- [x] Подтверждено, что `VectorEramSelector`, порт `0x20` и ERAM-ветви `VectorAddrSpace` образуют штатный режим `CFG_EDD=ERAM`, а не мёртвый код.
- [x] Исправлена ошибочная запись итерации 1.78 об удалении `VectorEramSelector`.
- [x] ERAM-код не изменялся и не удалялся.
- [ ] Подключение ERAM к статической конфигурации требует отдельного архитектурного решения о выборе между EDD/EDD2 и ERAM.


## Итерация 1.90 — прямой типизированный доступ к VectorAddrSpace

- [x] `VectorCore` сохраняет указатель на единственный создаваемый `VectorAddrSpace` и предоставляет типизированный `getAddrSpace()`.
- [x] `VectorFileLoader` и `VectorKbdLayout` больше не восстанавливают конкретный тип адресного пространства через общий `Cpu::getAddrSpace()` и `static_cast`.
- [x] Создание и подключение адресного пространства к CPU, основная RAM, ROM, EDD/EDD2 и сохранённый ERAM-тракт не изменены.
- [x] Поведение загрузки `.fdd`, обычных файлов и клавиш F11/F12 не изменено.


## Итерация 1.91 — удалить STL-владение фиксированным составом VectorCore

- [x] `VectorCore::addDevice()` и общий `std::vector<std::unique_ptr<EmuObject>> m_devices` удалены.
- [x] Фиксированный состав единственной машины создаётся явно через `new`, а каждый объект сохраняется в отдельном поле владельца.
- [x] `init()`, `reset()`, `shutdown()` и деструктор содержат явный порядок обработки всех устройств; порядок соответствует прежнему порядку `m_devices`, удаление выполняется в обратном порядке.
- [x] Фиксированный список из десяти tape hooks заменён обычным массивом; из `Vector.h` удалены зависимости от `<memory>`, `<utility>` и `<vector>`.
- [x] Конфигурация устройств, CPU, память, видео, FDD A/B, HDD, EDD/EDD2 и сохранённый ERAM-тракт не изменены.

## Итерация 1.92 — типизированные поля устройств VectorCore и исправление массива hooks

- [x] Все поля владения фиксированными устройствами `VectorCore` получили фактические типы вместо общего `EmuObject*`.
- [x] Удалены дублирующие указатели на те же объекты (`m_device_*` и отдельные рабочие поля); каждый объект теперь хранится ровно в одном типизированном поле.
- [x] Исправлено переключение tape hooks после замены `std::vector` на фиксированный массив: используются `m_tapeHooks[0]` и range-for без вызовов `.empty()`/`.front()`.
- [x] Удалён ошибочно продублированный блок полей устройств внутри `VectorAddrSpace`, попавший в предыдущий патч.
- [x] Порядок создания, `init()`, `reset()`, `shutdown()` и удаления устройств не изменён; ERAM/EDD/EDD2 не затронуты.

## Итерация 1.93 — прямое создание типизированных устройств VectorCore

- [x] Удалены временные локальные указатели конструктора, которые дублировали все поля `m_*` и существовали только до конца `VectorCore::VectorCore()`.
- [x] Все 52 устройства создаются, настраиваются и связываются непосредственно через единственные типизированные поля `VectorCore`.
- [x] Исключено смешение двух имён одного объекта при подключении CPU, памяти, видео, звука, накопителей и tape hooks.
- [x] Состав машины, порядок создания, `setMachine()`, подключения, `init()`, `reset()`, `shutdown()` и удаления не изменены.
- [x] EDD/EDD2 и сохранённый ERAM-тракт не изменены; новых STL-зависимостей не добавлено.

## Итерация 1.94 — откат регрессии 1.93

- [x] Полностью откатаны изменения `VectorCore::VectorCore()` из итерации 1.93.
- [x] Восстановлена схема с локальными типизированными указателями и последующим присвоением в поля `m_*`.
- [x] Причина отката: после 1.93 пропал обычный биперный звук и появились неожиданные зависания; итерация признана поведенчески небезопасной.
- [x] Изменения 1.91 и 1.92 — явное владение, типизированные поля, явный жизненный цикл и исправленный массив `m_tapeHooks` — сохранены.
- [x] ERAM/EDD/EDD2 не изменены.


## Итерация 1.95 — удалить оставшееся STL-владение корневыми объектами и фиксированными redirector-ами

- [x] `Emulation` больше не использует `std::unique_ptr` для четырёх объектов с фиксированным владельцем: `SoundMixer`, `WavReader`, `PrnWriter` и единственного `VectorCore`.
- [x] Создание, раннее удаление машины при ошибке окна/`SR_CLOSE` и деструктор переведены на явные `new`/`delete`; прежний порядок уничтожения `VectorCore` → `PrnWriter` → `WavReader` → `SoundMixer` сохранён.
- [x] `CloseFileHook` больше не содержит `std::vector`: каждый из трёх hooks получает ровно два существующих `TapeRedirector*` в конструкторе и хранит их в фиксированном массиве.
- [x] Из `Emulation.h` удалён `<memory>`, из `CloseFileHook.h` удалён `<vector>`; динамическая регистрация активных устройств `Emulation` не изменена.
- [x] Конструктор и жизненный цикл устройств `VectorCore`, звук, tape hooks, FDD A/B, HDD, EDD/EDD2 и ERAM-тракт не изменены.

## Итерация 1.96 — удалить фиктивное наследование от EmuObject у независимых классов

- [x] `CpuWaits` и `Ppi8255Circuit` больше не наследуются от `EmuObject`; для полиморфного удаления через собственные интерфейсы добавлены виртуальные деструкторы.
- [x] `AtaDrive`, `PrnWriter` и `WavReader` больше не наследуются от `EmuObject`: эти классы не используют `m_machine`, `m_kDiv`, `setFrequency()`, `init()` или `shutdown()` базового класса.
- [x] Удалены бессмысленные вызовы `setMachine()` для `VectorPpi8255Circuit`, `VectorPpi8255Circuit2`, `AtaDrive` и `VectorCpuWaits`; их поведение и связи задаются собственными типизированными методами.
- [x] Наследование сохранено у классов, реально использующих состояние или жизненный цикл `EmuObject` (`VectorRenderer`, `DiskImage`, `RamDisk`, hooks, клавиатура, sound sources и адресуемые/активные устройства).
- [x] Граф устройств, порядок создания/инициализации/сброса/удаления, звук, ERAM, EDD и EDD2 не изменены.

## Итерация 1.97 — исправить ошибочное удаление EmuObject в 1.96

- [x] Возвращено наследование `EmuObject` для `Ppi8255Circuit`: `VectorPpi8255Circuit` использует `m_machine`, а оба PPI-объекта участвуют в общем `init/reset/shutdown`.
- [x] Возвращено наследование `EmuObject` для `AtaDrive`: объект участвует в общем `init/shutdown`, а `reset()` снова объявлен как override.
- [x] Возвращено наследование `EmuObject` для `CpuWaits`: `VectorCpuWaits` участвует в общем `init/reset/shutdown`.
- [x] В конструктор `VectorCore` возвращены необходимые `setMachine(this)` для PPI, Covox PPI, ATA и CPU waits.
- [x] Удаление фиктивного наследования сохранено только для действительно независимых `PrnWriter` и `WavReader`.
- [x] Звук, граф устройств, ERAM, EDD и EDD2 не изменены.

## Итерация 1.98 — убрать STL из реестра активных устройств Emulation

- [x] Динамический `std::vector<IActive*>` заменён фиксированным массивом из 32 указателей; для статического состава единственной машины динамическое изменение ёмкости не требуется.
- [x] `registerActiveDevice()` явно добавляет устройство в конец массива и сохраняет прежний порядок регистрации.
- [x] `unregisterActiveDevice()` явно находит устройство и сдвигает последующие элементы, сохраняя прежнее поведение `vector::erase`.
- [x] Из `Emulation.h` удалён `<vector>`, из `Emulation.cpp` удалён `<algorithm>`.
- [x] При превышении фиксированного лимита выводится ошибка и запрашивается завершение вместо выхода за границы массива.
- [x] Алгоритм выбора ближайшего активного устройства, прерывание текущего прохода через `inCycle`, граф VectorCore, звук, ERAM, EDD и EDD2 не изменены.


## Итерация 1.99 — убрать динамические STL-контейнеры из звука и сигнатур CPU hooks

- [x] `SoundMixer` больше не использует `std::list<SoundSource*>`: источники хранятся в фиксированном массиве из 8 указателей с явным количеством.
- [x] Порядок добавления, микширования и удаления источников сохранён; при удалении оставшиеся указатели сдвигаются так же, как в прежнем списке.
- [x] При превышении лимита источников выводится ошибка и запрашивается завершение вместо выхода за границы массива.
- [x] `CpuHook` больше не использует `std::vector<uint8_t>` и отдельный указатель на его данные: сигнатура хранится непосредственно в массиве из 16 байт.
- [x] Парсинг шестнадцатеричных сигнатур выполняется напрямую без `std::stringstream`, временных строк и динамических выделений памяти.
- [x] Все десять текущих сигнатур Vector-06C имеют длину 6 байт и укладываются в установленный лимит.
- [x] Порядок микширования, проверка сигнатур, граф устройств, ERAM, EDD и EDD2 функционально не изменены.


## Итерация 2.00 — откат регрессивной замены списка источников звука

- [x] После применения итерации 1.99 зафиксирована регрессия звука.
- [x] Изменения `SoundMixer` из итерации 1.99 полностью отменены: восстановлен `std::list<SoundSource*>` и прежние `push_back`, `remove` и итерация по списку.
- [x] Независимая переработка фиксированных сигнатур `CpuHook` из итерации 1.99 сохранена.
- [x] Граф устройств, порядок их создания и удаления, ERAM, EDD и EDD2 не изменены.

## Итерация 2.01 — полноценный reset по F11

- [x] Обычный F11 больше не ограничивается �xb2ключением ROM и `PC=0`: это оставляло CPU, IRQ, таймеры и периферию в текущем состоянии.
- [x] F11 теперь вызывает полный `VectorCore::reset()` с временной блокировкой reset клавиш, как и F12.
- [x] F12 сохраняет своё отличие: после reset отключает ROM.
- [x] ERAM/EDD/EDD2 и звуковой тракт не изменены.


## Итерация 2.03 — повторный тест фиксированного реестра источников SoundMixer

- [x] Изолированно повторена только замена `std::list<SoundSource*>` на фиксированный массив из 8 указателей; изменения жизненного цикла устройств из откатанной итерации 1.93 не возвращались.
- [x] Сохранены порядок добавления и микширования источников, удаление со сдвигом и явная защита от переполнения массива.
- [x] Повторный тест выполняется с учётом найденного штатного сценария: после ручной загрузки FDD через F12 основной ВВ55 сохраняет состояние из-за `noReset`, поэтому отсутствие биперного звука само по себе не считается регрессией `SoundMixer`.
- [x] Проверен звук при чистом запуске ROM без предварительной загрузки FDD.
- [x] Проверен звук после подключения FDD через Alt+A без запуска с дискеты.
- [x] Версия регрессии опровергнута: фиксированный реестр источников звука работает штатно; прежнее исчезновение звука объяснялось отдельно найденной последовательностью FDD → F12 и сохранением состояния ВВ55.
- [x] ERAM, EDD/EDD2, FDD, CPU hooks и reset-логика не изменены.


## Итерация 2.04 — убрать std::set из состояния клавиатурной раскладки

- [x] Два динамических `std::set<EmuKey>` заменены фиксированными таблицами присутствия клавиш и явными счётчиками.
- [x] Сохранена семантика множества: повторное нажатие не увеличивает счётчик, повторное отпускание не уменьшает его.
- [x] Переходы 0→1 и 1→0 для виртуальных SHIFT/LANG сохранены без изменения.
- [x] `resetKeys()` явно очищает обе таблицы и оба счётчика.
- [x] Проверены обычный ввод, Shift-комбинации и переключение языка в SMART-режиме; итерация принята.
- [x] Звук, FDD, reset, ERAM, EDD/EDD2 и граф устройств не изменены.


## Итерация 2.05 — крупный блок статизации конструктора, адресных диапазонов и CPU hooks

- [x] Повторно возвращена целиком ранее откатанная итерация 1.93: `VectorCore::VectorCore()` создаёт устройства непосредственно в типизированных полях `m_*`, без параллельного набора локальных указателей.
- [x] Все связи между устройствами в конструкторе теперь задаются через поля-владельцы; порядок создания, настройки, `init()`, `reset()` и показа окна сохранён.
- [x] `AddrSpace` больше не использует восемь параллельных `std::vector`: диапазоны чтения и записи хранятся в двух фиксированных массивах структур по 32 записи.
- [x] Сохранены сортировка диапазонов по начальному адресу, приоритет последнего подходящего диапазона и преобразование адреса устройства.
- [x] Реестр `Cpu8080Compatible` больше не использует `std::vector`, `std::map` и динамически создаваемые `std::list`: до 32 hooks хранятся в одном фиксированном массиве.
- [x] Проверка hooks выполняется в прежнем порядке регистрации; после первого `hookProc() == true` дальнейшие hooks того же адреса не вызываются, как и раньше.
- [x] В блок включены сразу три независимые статизации, поскольку полный функциональный тест дороже разработки и возможного отката.
- [x] Общий тест пройден: загрузка ROM/FDD, F11/F12, клавиатура, звук и основные подсистемы работают штатно; итерация принята.


## Итерация 2.06 — крупный блок статизации ввода, файлового диалога и RAM-дисков

- [x] Динамическая `std::queue` указателей на клавиатурные события и отдельные `new/delete` для каждого события заменены кольцевым буфером из 64 значений `PalKeyCodeAction`.
- [x] `getKey()` и `processKeys()` используют единое извлечение из очереди; порядок событий сохранён.
- [x] Файловый диалог больше не создаёт отдельный `PalFileInfo` для каждой записи и не хранит указатели в `std::vector`/`std::list`: используется фиксированный массив из 256 записей.
- [x] Сортировка каталогов и файлов, переход по каталогам, прокрутка и выбор имени сохранены; лишний публичный overload `palGetDirContent()` удалён.
- [x] `RamDisk` специализирован под фактически используемую конфигурацию Вектора: один RAM-блок на каждый EDD/EDD2 вместо динамического массива страниц.
- [x] Удалены выделение массива `SRam**`, счётчик страниц и циклы по единственной странице; формат файлов RAM-диска остаётся 256 КиБ.
- [x] Изменения объединены в один тестовый блок, поскольку полный функциональный прогон дороже разработки и возможного отката.
- [x] Общий тест пройден; итерация принята.


## Итерация 2.07 — reset перед загрузочным прогоном FDD

- [x] После выбора `.fdd` через loader образ сначала назначается дисководу A, затем машина полностью сбрасывается.
- [x] Предварительный запуск loader ROM больше не выполняется поверх состояния ранее запущенного `.rom`-файла.
- [x] На время reset блокируется сброс состояния клавиш, как в обработчиках F11/F12.
- [x] Существующая последовательность `enableRom()`, `PC=0` и 25-миллионный тактовый прогон loader ROM сохранена.
- [x] Проверен сценарий: запуск `.rom` → Alt+F3 → выбор `.fdd` → Enter; зависание core0 устранено, итерация принята.


## Итерация 2.08 — встроить локальные объекты фиксированного состава

- [x] `PalFile` на Pico больше не выделяет отдельный `FIL` при каждом открытии: структура FatFs хранится непосредственно внутри объекта, а состояние открытия задаётся явным флагом.
- [x] Повторный `open()` предварительно закрывает ранее открытый файл; чтение, запись, позиционирование и проверка EOF используют адрес встроенного `FIL`.
- [x] `Pit8253` больше не создаёт и не удаляет три `Pit8253Counter` динамически: счётчики встроены в объект таймера, а прежний массив указателей сохранён как совместимый индексный доступ.
- [x] `WavReader` больше не создаёт отдельный `WavSoundSource` через `new/delete`: источник звука является непосредственным полем и получает ссылку на владельца в списке инициализации.
- [x] Порядок регистрации источника в `SoundMixer`, доступ к счётчикам PIT и файловые форматы не изменены.
- [x] Общий тест файлового диалога/ROM/FDD, WAV-воспроизведения и звука PIT пройден.


## Итерации 2.09–2.10 — неудачная статизация сервисов Emulation и полный откат

- [x] Попытка встроить `SoundMixer`, `WavReader` и `PrnWriter` больше не создаются и не удаляются через `new/delete`: три сервиса фиксированного состава являются непосредственными полями `Emulation`.
- [x] Публичные getters сохраняют прежний интерфейс указателей и возвращают адреса встроенных объектов, поэтому существующие пользователи сервисов не изменены.
- [x] Динамическим остаётся только `VectorCore`, поскольку машина может быть досрочно уничтожена по `SR_CLOSE` и при ошибке создания окна.
- [x] Порядок уничтожения сохранён по смыслу: сначала явно удаляется `VectorCore`, затем автоматически разрушаются `PrnWriter`, `WavReader` и `SoundMixer` в обратном порядке объявления.
- [x] Настройка частоты, sample rate, громкости, WAV-загрузка, печать и mute используют те же объекты и тот же порядок вызовов.
- [x] После изменения прошивка не стартовала; 2.09 полностью отменена итерацией 2.10.
- [x] `SoundMixer`, `WavReader` и `PrnWriter` снова создаются раздельно через `new/delete`; поведение восстановлено.


## Итерация 2.11 — удалить мёртвую инфраструктуру общего CRT renderer

- [x] Из `CrtRenderer` удалена неиспользуемая поддержка primary/secondary renderer, оставшаяся от мультиэмуляторной архитектуры.
- [x] Удалены два неиспользуемых указателя framebuffer и соответствующие `delete[]`; активный буфер `m_pixelData` сохранён.
- [x] Удалены неиспользуемые поля старой схемы двойной буферизации и текстового экрана.
- [x] Удалён невостребованный `generateTextScreen()` вместе с `codecvt`, временными динамическими массивами и строкой результата.
- [x] Конструктор `VectorRenderer` теперь задаёт только реально используемые размеры framebuffer.
- [x] Удалена нигде не вызываемая `palReadFile()`, выделявшая буфер размером с файл; частичное чтение `palReadFromFile()` сохранено.
- [x] Общий тест пройден; итерация принята.


## Итерация 2.12 — удалить неработающие desktop-возможности

- [x] Удалён `SR_FONT`: renderer Вектора не переопределял смену метода/шрифта, поэтому команда всегда была пустой.
- [x] Удалены `SR_COPYTXT` и `SR_PASTE` вместе с локальным псевдобуфером обмена Pico; текстовый экран уже отсутствует, а вставка не получала данные из внешней системы.
- [x] Удалена цепочка `mouseDrag` от окна до renderer: у renderer Вектора обработчика не было, вызов заканчивался пустым virtual-методом.
- [x] Активные команды цвета, cropping, клавиатуры и файловых операций не изменены.
- [x] Общий тест пройден; итерация принята.


## Итерация 2.13 — удалить мёртвую оконную интеграцию desktop PAL

- [x] Удалены неиспользуемые callback-и focus и drag-and-drop файлов из `EmuCalls` и `Emulation`.
- [x] Удалены пустые `restoreFocus()`/`bringToFront()` и вызовы возврата фокуса после файловых диалогов.
- [x] Удалены caption/title и параметры desktop-окна, которые Pico PAL нигде не применял.
- [x] Удалены неиспользуемые объявления maximize, windowById, focusChanged и mouseClick.
- [x] Видимость окна, файловые диалоги, клавиатура, SysReq и активный видеотракт не изменены.
- [x] Общий тест запуска, файловых диалогов, ROM/FDD/WAV и горячих клавиш пройден; итерация принята.

## Итерация 2.14 — убрать лишние копирования строк в активных трактах

- [x] Параметры имён файлов, фильтров, режимов открытия, сообщений и вводимого текста переведены с передачи `std::string` по значению на `const std::string&`.
- [x] `DiskImage::getLabel()` и `RamDisk::getLabel()` возвращают константную ссылку вместо создания копии строки.
- [x] Изменения охватывают ROM/RAM, FDD/HDD, магнитофон, файловый PAL, журнал, сообщения и клавиатурный tapper.
- [x] Владение строками и момент копирования в поля объектов сохранены; время жизни временных аргументов не выходит за пределы вызова.
- [x] Размещение объектов, heap, порядок конструирования и активная логика устройств не изменены.
- [x] Общий тест пройден; итерация принята.

## Итерация 2.15 — удалить неиспользуемые файловые и диагностические интерфейсы

- [x] Удалён неиспользуемый `WavReader::posToTime()` вместе с форматированием времени через `stringstream`.
- [x] Из `PrnWriter` удалены неиспользуемые permanent filename, `getReady()` и `getFileName()`; запуск печати всегда использует активный файловый диалог, как и в текущем Pico UI.
- [x] Из `PalFile` удалены нигде не вызываемые статические операции `create`, `del`, `mkDir` и `moveRename`.
- [x] Активные open/read/write/seek/close, печать байтов, WAV-воспроизведение и файловый диалог не изменены.
- [x] Изменения собираются; отдельный функциональный тест не требовался, поскольку активная логика не менялась.


## Итерация 2.16 — удалить мёртвые ветки TapeRedirector

- [x] Удалён таймер автоматического закрытия файла: `m_timeout` нигде не задавался и всегда оставался равен нулю, поэтому `CloseFileTimer` никогда не создавался.
- [x] Удалены неиспользуемые `assignFile()` и permanent filename; оба магнитофонных редиректора всегда работают через файловый диалог.
- [x] Удалены неиспользуемые внешние методы `setFilePos()`, `readByteSkipSeq()`, `skipSeq()`, `isLvt()` и `isTsx()`.
- [x] Активные пути чтения/записи ленты, WAV/CSW, последовательных LVT и разбора TSX сохранены.
- [x] Общий тест загрузки и сохранения ленты, WAV/CSW и доступных TSX/LVT пройден; итерация принята.


## Итерация 2.17 — удалить константное и только записываемое состояние

- [x] Удалены поля `DiskImage::m_permanentFileName` и `m_autoMount`, не имевшие ни одного использования.
- [x] Удалена неиспользуемая трансляция координат мыши `EmuWindow::translateCoords()` и связанные поля размеров/прямоугольника; мышиная цепочка была удалена ранее.
- [x] Удалён константный флаг `CrtRenderer::m_defaultDebugRendering`; сохранён фактически исполнявшийся debug-rendering путь.
- [x] Удалён неиспользуемый флаг ATA vector geometry; явная начальная настройка геометрии сохранена.
- [x] Удалены только записываемые номер счётчика PIT, состояние языка клавиатуры, latched 512px/scanline clock renderer и число дорожек FDD.
- [x] Модель памяти, порядок конструирования и активные алгоритмы устройств не изменены.
- [ ] Выполнить общий тест запуска, изображения, AY/PIT-звука, клавиатуры и FDD/HDD.


## Итерация 2.18 — восстановить переключаемый stereo/mono PSG

- [x] Возвращены `Psg3910SoundSource::m_stereo` и условные ветки stereo ABC / mono.
- [x] Функциональность сохранена для последующего вывода настройки stereo/mono в меню.
- [ ] Добавить пункт меню настройки PSG stereo/mono на отдельном этапе интерфейса.


## Итерация 2.19 — исправить регрессию обработки клавиатуры

- [x] Удалён оставшийся пустой `else if (emuKey == EK_LANG)` после удаления `m_langPressed`.
- [x] `kbd->processKey(emuKey, isPressed)` снова вызывается безусловно для всех переведённых клавиш в ветке `KLM_SMART`.
- [x] Исправлена регрессия 2.17, при которой в этой ветке до клавиатуры доходила только клавиша языка.
- [ ] Проверить обычные клавиши, Shift и переключение языка в режиме SMART.


## Итерации 2.18–2.19 — восстановить stereo и исправить dispatch клавиатуры

- [x] Восстановлены `Psg3910SoundSource::m_stereo` и обе ветки Stereo ABC / Mono; переключатель должен быть позднее выведен в меню.
- [x] Удалён оставшийся пустой `else if (emuKey == EK_LANG)`, из-за которого `kbd->processKey()` стал условным.
- [x] Общий тест звука и клавиатуры пройден; исправления приняты.


## Итерация 2.20 — удалить заведомо недействующие остатки интерфейсов

- [x] Удалено объявление `AtaDrive::setReadOnly()`, для которого не существовало реализации и вызывающего кода.
- [x] Удалены закомментированные остатки пошагового debug-флага CPU.
- [x] Удалена desktop-структура `DebuggerOptions`, которая только заполнялась константами и нигде не читалась.
- [x] Удалены неиспользуемые `debugRun()` и getter vsync; активные `debugRequest()`, остановка CPU и установка vsync сохранены.
- [x] Удалены необъявленные к использованию `startCount()`, `stopCount()` и `checkForInt()` PIT, не имевшие реализаций.
- [x] Удалён невостребованный `Pit8253Counter::getStats()`; активная статистика для генерации звука не изменена.
- [x] Потенциально полезные getters и переключатели устройств сохранены для будущего меню.
- [x] Проверить сборку; отдельный функциональный тест не требуется.


### Итерация 2.21 — удаление мёртвого интерфейса вывода и PAL-обёрток

Статус: принята; сборка и функциональная проверка пройдены.

Изменения:

- удалены неиспользуемые PAL-обёртки `emuResetKeys()`, `emuSysReq()` и `emuGetEmulationSpeedFactor()`;
- удалены не вызывавшиеся `EmuWindow::hide()`, `drawFrame()`, `drawOverlay()` и `endDraw()` вместе с `m_frameDrawn`;
- удалён неиспользуемый экспорт кадра `CrtRenderer::getPixelData()`, одноразовый счётчик обновления и мёртвый `m_frameNo`;
- удалён не вызывавшийся `Cpu8080Compatible::removeHook()`;
- удалён оставшийся закомментированный прототип `VectorRenderer::advance()`;
- активные рендеринг, переключение буферов, обработка клавиатуры, SysReq и регистрация CPU hooks не изменены.

Проверка:

- [x] сборка firmware;
- [x] функциональная проверка пройдена.


### Итерация 2.22 — удалить пустой жизненный цикл desktop-окна

Статус: подготовлена, ожидает сборочной проверки.

Изменения:

- удалены пустые `PalWindow::applyParams()`, `drawEnd()`, `drawImage()`, `drawFill()` и `getSize()`;
- удалены неиспользуемые параметры видимости и расчёт прямоугольника desktop-окна;
- удалены пустые `EmuWindow::init()` и `show()` вместе с их вызовами из `VectorCore`;
- удалён ставший пустым `picoPalWindow.cpp`;
- обработка `SR_SCREENSHOT` и реальный видеовывод не изменены.

Проверка:

 - сборка firmware;
 - отдельного функционального теста не требуется.


## Итерация 2.23 — удалить невостребованные классы старых машин и таймера

- [x] Удалены `ElapsedTimer` и `CloseFileTimer`, оставшиеся без единого пользователя после удаления неработавшего tape timeout в 2.16.
- [x] Удалён `NullSpace`, который нигде не создавался и не участвовал в карте адресного пространства Вектора.
- [x] Удалён `Cpu8080StatusWordSpace`, не имевший экземпляров и подключений к портам или памяти.
- [x] Удалён специализированный `RkPit8253SoundSource` для Радио-86РК; активный `Pit8253SoundSource` Вектора сохранён.
- [x] Удалён невостребованный `KrKbdLayout`; активные раскладки и обработка клавиатуры Вектора не изменены.
- [x] Сборка и общий тест пройдены; итерация принята.


## Итерация 2.24 — удалить невостребованный debug-renderer и мелкие мёртвые интерфейсы

- [x] Удалены `prepareDebugScreen()` из `CrtRenderer` и `VectorRenderer`: во всём дереве не было ни одного вызывающего кода.
- [x] Удалены флаг `reqForSwapBuffers` и `enableSwapBuffersOnce()`, которые могли включаться только из удалённых методов.
- [x] `swapBuffers()` сохраняет активное поведение: при pause/debug кадр не обновляется; недостижимое исключение из этого правила удалено.
- [x] Удалён большой закомментированный остаток старой desktop-схемы двойной буферизации.
- [x] Удалены невостребованный `ffPalFile::write16()` и старые закомментированные объявления CPU/clock getters.
- [ ] Проверить сборку и обычный видеовывод; отдельно проверить pause/debug, если эти режимы сейчас доступны.

### Итерация 2.25 — убрать остаток многоконного клавиатурного диспетчера

Статус: принята; сборка и функциональная проверка пройдены.

- Передавать клавиатурные события непосредственно единственной машине VectorCore.
- Удалить сравнение окна и недостижимый fallback в EmuWindow::processKey().
- Упростить resetKeys(): сброс модификаторов и клавиатуры единственной машины без параметра окна.
- Сохранить сигнатуру PAL-callback emuKeyboard(), но больше не использовать переданный PalWindow*.


## Итерация 2.26 — убрать остатки desktop-клавиатурного интерфейса окна

- [x] Удалён пустой `EmuWindow::processKey()`: после 2.25 клавиатурные события передаются непосредственно единственной машине `VectorCore`.
- [x] Из `EmuWindow.h/.cpp` удалены ставшие ненужными keyboard/desktop include, forward declaration и `using namespace std`.
- [x] Удалены старые резервные `util_Wii_Joy.c.bak` и `util_Wii_Joy.h.bak`, не участвовавшие в сборке.
- [x] Удалён дублирующий недостижимый `break` в обработке `SR_SPEEDSTEPDOWN`.
- [x] Активные `EmuWindow::sysReq()`, screenshot и системные сочетания не изменены.
- [ ] Проверить сборку; отдельный функциональный тест не требуется.


## Итерация 2.28 — удалить невызванный общий диспетчер SysReq

- [x] Удалён `Emulation::sysReq(EmuWindow*, SysReq)`: после перехода на единственную машину во всём дереве не осталось ни одного вызова этого метода.
- [x] Удалены недостижимые ветки закрытия произвольного окна, дублирующей передачи SysReq окну и машине, а также пустой `EmuWindow::closeRequest()`.
- [x] Активный путь системных сочетаний сохранён: `machineKey()` напрямую вызывает `VectorCore::sysReq()`, а screenshot обрабатывается `EmuWindow::sysReq()`.
- [x] Итерации 2.26 и 2.27 отмечены как применённые; ошибка include `palOpenFileDialog()` исправлена.
- [ ] Проверить сборку и системные сочетания Alt+F1/F2/F3/F4, pause/speed и screenshot.

### Итерация 2.26 — убрать остатки оконной клавиатуры и резервные Wii-файлы

Статус: принята; сборка после исправления include пройдена.

### Итерация 2.27 — восстановить объявление диалога сохранения screenshot

Статус: применена вручную; сборка пройдена.

### Итерация 2.28 — удалить невызванный общий SysReq-диспетчер

Статус: принята; сборка и функциональная проверка пройдены.

### Итерация 2.29 — удалить невызванный PAL-жизненный цикл

Статус: принята; изменения применены.

Изменения:

- удалить невызванные `palQuit()` и `palIdle()`;
- удалить пустую и невызванную `palUpdateConfig()`;
- удалить оставшееся объявление `palWxProcessMessages()`, доступное из pico-заголовка только через удалённый `palIdle()`;
- активные инициализация PAL, основной цикл, файловые диалоги, звук и завершение через `palRequestForQuit()` не изменять.

Проверка:

- [ ] сборка firmware;
- [ ] отдельный функциональный тест не требуется.


### Итерация 2.30 — удалить остатки старой схемы регулировки скорости

Статус: принята; изменения применены.

Изменения:

- удалён невызванный `Emulation::setTemporarySpeedUpFactorDbl(double)`;
- удалён невызванный `Emulation::setSpeedByGrade(int)`;
- удалено неиспользуемое поле `m_speedGrade`;
- действующий `setTemporarySpeedUpFactor(unsigned)` и расчёт текущей частоты не изменены.

### Итерация 2.31 — удаление закомментированных фрагментов старой реализации

Статус: подготовлена, ожидает применения и проверки.

- удалены закомментированные объявления старых конструкторов, `exec()` и полей состояния;
- удалены закомментированные ветки прежнего отладочного шага CPU;
- удалены остатки старых реализаций PIT, FDC, tape redirector и видеобуфера;
- поясняющие комментарии, TODO и документация аппаратного поведения сохранены;
- исполняемый код не изменён.


### Итерация 2.31 — удалить устаревший закомментированный код

Статус: принята; изменения применены.


### Итерация 2.32 — удалить невызванный внешний интерфейс дополнительных регистров Z80

Статус: подготовлена, ожидает применения и проверки.

Изменения:

- удалить невызванные геттеры альтернативных банков `AF'`, `BC'`, `DE'`, `HL'`;
- удалить невызванные геттеры `IX`, `IY`, `I`, `IM`, `IFF`;
- удалить невызванные сеттеры альтернативных банков, `IX` и `IY`;
- внутренние поля регистров и логика исполнения инструкций Z80 не изменяются;
- основной интерфейс CPU (`AF`, `BC`, `DE`, `HL`, `SP`, `PC`, `setIFF`) сохраняется.

Проверка:

- [ ] запуск Z80-конфигурации и базовая проверка исполнения.
### Итерация 4.1 — каркас иерархического меню

- [x] Добавлен модальный каркас меню для Pico без возврата удалённой оконной подсистемы.
- [x] Одиночное нажатие Alt открывает корневое меню; существующие Alt-комбинации продолжают обрабатываться прежним путём.
- [x] Добавлены корневые разделы: Processor, Storage, ROM, Sound, Tape, Snapshots, Video, System и About.
- [x] Реализованы переход в подменю по Enter, возврат по Esc, закрытие по Alt и циклическая навигация Up/Down.
- [x] На время меню сохраняются и временно заменяются состояния pause и mute; при закрытии прежние состояния восстанавливаются.
- [x] Состояние клавиатуры очищается при входе и выходе, чтобы Alt и навигационные клавиши не оставались нажатыми в гостевой машине.
- [x] Область framebuffer под меню сохраняется и восстанавливается; видеорежим и renderer Вектора не изменены.
- [ ] Аппаратная сборка и проверка USB/PS2, открытия/закрытия и восстановления звука/паузы ожидаются.
- [ ] Содержимое разделов и интеграция существующего файлового менеджера будут добавляться следующими итерациями.


### Итерация 4.25 — загрузка физического CPU полезной работой

Статус: подготовлена, ожидает применения и проверки.

- [x] Возвращён серый недоступный пункт `CPU Load` рядом с FPS.
- [x] Полезным считается проход `exec()`, в котором был вызван хотя бы один `operate()` гостевого CPU или периферийного устройства.
- [x] Время проходов без единого вызова устройства считается idle и в загрузку не входит.
- [x] Загрузка усредняется в том же реальном окне 0,5 с, что и FPS.
- [x] Дополнительные чтения аппаратного таймера не добавлены внутрь циклов команд CPU и аудиоустройств.
- [ ] Проверить CPU Load на разных частотах гостевого CPU и при открытом меню.


### Итерация 4.26 — независимое включение EDD и EDD2

Статус: подготовлена, ожидает применения и проверки.

- [x] В подменю `EDD` и `EDD2` добавлен симметричный переключатель `Enabled`.
- [x] Отключение закрывает гостевой машине доступ через селектор соответствующего порта, но не уничтожает память RAM-диска и не выгружает образ.
- [x] Переключение выполняется без `shutdown()`, `coldReinitialize()` и reset машины.
- [x] `Load image` и `Save image as` доступны также для отключённого EDD/EDD2.
- [x] Старый отдельный переключатель `System -> EDD` удалён.
- [ ] Проверить включение/отключение обоих портов, сохранение содержимого и загрузку образа в отключённый диск.


### Итерация 4.27 — командный переключатель EDD/EDD2 без закрытия меню

Статус: подготовлена, ожидает применения и проверки.

- [x] Строка переключателя показывает действие `Disable` для включённого диска и `Enable` для отключённого.
- [x] После переключения подменю остаётся открытым, выделение сохраняется на той же строке.
- [x] Заголовок строки немедленно обновляется без закрытия каскада меню.
- [x] Остальные команды меню по-прежнему закрывают меню после выполнения.
- [ ] Проверить многократное переключение EDD и EDD2 клавишами Enter/Space без выхода из подменю.


### Итерация 4.28 — предварительный режим Read only для FDD

Статус: принята; изменения применены.

- [x] `Read only` доступен в подменю FDD даже без вставленного образа.
- [x] Для приводов A и B режим хранится отдельно от текущего образа.
- [x] Выбранный режим передаётся в диалог открытия нового образа.
- [x] `Eject` и смена образа не сбрасывают предварительно выбранный режим.
- [x] Переключение `Read only` не закрывает каскад меню и сохраняет курсор.

### Итерация 4.29 — постоянный суффикс режима FDD

Статус: принята; изменения применены.

- [x] Режим FDD всегда отображается суффиксом одинаковой длины: `(ro)` или `(rw)`.
- [x] Ширина подменю заранее учитывает суффикс и не меняется при переключении.

### Итерация 4.30 — меню Sound и выбор физического выхода

Статус: выполняется поэтапно; переключение физического выхода реализовано в итерации 4.32.

- [x] В главном меню показывать не нейтральное `Sound`, а текущий физический выход: `Sound (I2S)` или `Sound (PWM)`.
- [x] Заголовок должен обновляться сразу после смены выхода без закрытия каскада меню.
- [x] В подменю `Sound` добавить переключатель физического выхода `I2S / PWM`.
- [x] Переключение должно выполняться без reset гостевой машины: деинициализировать текущий способ вывода и затем инициализировать выбранный.
- [x] При деинициализации остановить передачу и освободить DMA, PIO/PWM/I2S и связанные аппаратные ресурсы текущего тракта.
- [x] При ошибке инициализации нового выхода восстановить предыдущий тракт и оставить его выбранным в меню.
- [x] Учесть compile-time режимы `AUDIO_FORCE_I2S` и `AUDIO_FORCE_PWM`: запрещённый сборкой вариант не должен предлагаться как доступный.
- [ ] Добавить общую регулировку громкости и отображение текущего значения.
- [ ] Добавить включение/выключение реально существующих звуковых источников по результатам инвентаризации.
- [ ] Для PSG добавить доступные режимы: mono/stereo и раскладку каналов `ABC / ACB`; не добавлять режимы, которых нет в тракте.
- [ ] Для каждого изменяемого пункта использовать постоянную ширину строки и, где это уместно, оставлять меню открытым после переключения.
- [ ] До реализации составить точный перечень источников, точек микширования, коэффициентов громкости и параметров, безопасно изменяемых на лету.


### Итерация 4.31 — уточнение жизненного цикла физического звукового выхода

Статус: реализована в итерации 4.32; ожидает аппаратной проверки.

- [x] Смена `I2S / PWM` не выполняет reset гостевой машины.
- [x] Текущий способ вывода полностью деинициализируется перед запуском другого.
- [x] Новый способ вывода инициализируется только после освобождения DMA и аппаратных ресурсов предыдущего.
- [x] Если новый тракт не удалось запустить, выполняется откат к предыдущему, а заголовок и переключатель меню показывают фактически активный выход.


### Итерация 4.32 — переключение физического выхода Sound

Статус: подготовлена, ожидает применения и аппаратной проверки.

- [x] Корневой пункт показывает фактически активный выход: `Sound (I2S)` или `Sound (PWM)`.
- [x] В подменю `Sound` добавлена командная строка `Switch to PWM` / `Switch to I2S`; переключение не закрывает меню.
- [x] Перед сменой тракта останавливается таймер выдачи, затем полностью деинициализируется текущий PWM или I2S.
- [x] PWM освобождает GPIO и выключает используемые slices; I2S освобождает PIO SM, программу, GPIO, DMA и буфер при их наличии.
- [x] Новый тракт инициализируется с текущей частотой дискретизации без reset гостевой машины.
- [x] При неудаче выполняется попытка восстановить прежний тракт; меню отражает фактически выбранный выход.
- [x] В сборках `AUDIO_FORCE_PWM` и `AUDIO_FORCE_I2S` строка выбора остаётся информационной и недоступной.
- [ ] Проверить на обеих платах многократные переходы PWM -> I2S -> PWM, отсутствие зависших DMA/PIO и сохранение работы гостевой машины.


### Итерация 4.33 — исправление выбора и переключения Sound

Статус: подготовлена, ожидает применения и аппаратной проверки.

- [x] Автоопределение не отвергает I2S-модуль только из-за внутренней подтяжки его входов: выбор основан на быстром нарастании обоих общих выводов.
- [x] I2S использует штатные `AUDIO_DATA_PIN` и `AUDIO_CLOCK_PIN`, а не выводы PWM.
- [x] На Murmulator 2 это исключает ошибочное занятие GPIO 12, с которого начинается видеошина VGA/HDMI.
- [x] В подменю показывается команда следующего действия: `Switch to PWM` при активном I2S и `Switch to I2S` при активном PWM.
- [ ] Проверить старт универсальной платы с I2S-модулем: ожидается автоматический выбор I2S.
- [ ] Проверить переходы PWM -> I2S -> PWM: изображение не должно изменяться или повреждаться.


### Итерация 4.34 — автодетект по сигнатуре GPIO и 12-битный PWM

Статус: подготовлена, ожидает применения и аппаратной проверки.

- [x] Автоопределение сверено с `murmulator-tester`: используются те же пассивные измерения уровней с pull-down и pull-up и задержкой 33 мс.
- [x] Проверяются штатные линии I2S `AUDIO_DATA_PIN` и `AUDIO_CLOCK_PIN`, а не только два PWM-выхода.
- [x] I2S выбирается для сигнатуры свободных цифровых входов `down=0, up=1` на обеих линиях; нагруженная аналоговая линия оставляет PWM.
- [x] PWM переведён с 8 на 12 бит: `wrap=4095`, преобразование signed 16-bit выполняется сдвигом на 4 бита.
- [x] Остаток квантования пересчитан для 12-битного выхода.
- [ ] Проверить автодетект на универсальной плате с обоими вариантами модуля.
- [ ] Сравнить субъективную громкость PWM и I2S на одинаковом миксе.
- [ ] Проверить отсутствие слышимой несущей PWM при фактической системной частоте сборки.

### Итерация 4.35 — полный активный testPins из pico-spec

Статус: подготовлена, ожидает применения и аппаратной проверки.

- [x] Автоопределение повторно сверено с рабочей реализацией `pico-spec`.
- [x] Исправлена ошибка итерации 4.34: рабочий `testPins` не ограничивается чтением pull-down/pull-up, а после пассивной классификации активно возбуждает одну линию и проверяет вторую.
- [x] Перенесены четыре поддерживаемые сигнатуры: `00->00`, `01->01`, `10->10` и `11->11`.
- [x] I2S выбирается по ненулевому полному коду `testPins(AUDIO_DATA_PIN, AUDIO_CLOCK_PIN)`.
- [x] Значения пассивной сигнатуры, активного результата и полного кода сохранены в `palAudioProbe()` для диагностики.
- [ ] Проверить холодный старт универсальной платы с I2S и PWM.

### Итерация 4.36 — диалог About

Статус: подготовлена, ожидает применения и проверки на VGA/TV-out.

- [x] Пустая страница `About` заменена модальным информационным диалогом.
- [x] Указано, что прошивка содержит порт платформы Vector-06C из многоплатформенного проекта emu80v4 для Murmulator 1.x / Murmulator 2.0 и Raspberry Pi Pico 2 (RP2350).
- [x] Указаны исходный проект, Victor Pykhonyn (Pyk), автор порта `@Michael_V1973` и страница сообщества Murmulator в Telegram.
- [x] Добавлена благодарность Victor Pykhonyn (Pyk) за вклад в сохранение и продвижение классических 8-битных платформ.
- [x] Дата и время сборки выводятся через `__DATE__` и `__TIME__`.
- [x] Координаты диалога учитывают смещение видимой области TV-out.
- [x] Диалог закрывается клавишами Enter, Space или Esc и возвращает пользователя в меню.
- [ ] Проверить размещение текста и восстановление меню на VGA и TV-out.


### Исправление 4.36c — About через штатный блокирующий ввод

Статус: подготовлено, ожидает применения и проверки.

- [x] Удалено отдельное состояние `s_aboutOpen` и специальная ветка обработки клавиш главного меню.
- [x] После отрисовки About блокирующе читает `getKey()` тем же способом, который уже используется модальным файловым менеджером Pico PAL.
- [x] После закрытия About действие возвращается в обычный обработчик пункта меню, который штатно закрывает весь каскад.
- [x] Новые функции и изменения интерфейса `picoPal.*` не добавлялись.
- [ ] Проверить закрытие About клавишами Enter, Space и Esc и возврат непосредственно в эмулятор.


### Итерация 4.37 — отображение джойстиков в клавиатурные события

- [x] Направления обоих джойстиков объединены и формируют те же события стрелок и навигации меню, что клавиатурный островок.
- [x] Кнопки A, B, SELECT и START отображаются соответственно в Space, Enter, Tab и левый Alt.
- [x] Для NES-пада восстановлено чтение кнопки SELECT первого контроллера.
- [x] События формируются только при изменении состояния кнопки; удержание и отпускание передаются как обычные клавиатурные нажатия.
- [ ] Проверить USB HID/XInput и NES-пады: направления, A/B, SELECT, открытие и закрытие меню кнопкой START.

### Исправление 4.37.2 — включение NES PAD и порядок направлений

Статус: подготовлено, ожидает применения и аппаратной проверки.

- [x] Добавлено `USE_NESPAD=1`: ранее драйвер линковался, но инициализация и опрос были исключены препроцессором.
- [x] Исправлен порядок полей в `input_bits_t`: направления больше не переставляются агрегатной инициализацией.
- [ ] Проверить NES PAD на `PICO_BOARD=murmulator`: стрелки, A/Space, B/Enter, SELECT/Tab, START/Alt.

### Исправление 4.37.3 — опрос джойстика в модальных диалогах

Статус: подготовлено, ожидает аппаратной проверки.

- [x] В циклы файлового диалога, сообщений, подтверждения перезаписи и About добавлен штатный `palInputTick()`.
- [x] NES PAD продолжает опрашиваться, пока основной цикл эмуляции остановлен модальным окном.
- [x] События направлений, A/Space, B/Enter и SELECT/Tab попадают в ту же очередь `getKey()`, которую читает файловый диалог.
- [ ] Проверить навигацию NES PAD по списку файлов, вход в каталог, выбор файла и переключение фокуса кнопкой SELECT.

### Исправление 4.38 — Sound: volume, stereo и PSG ABC/ACB

Статус: подготовлено, ожидает проверки на железе.

- [x] Общая громкость PWM/I2S выбирается из 7 штатных уровней `SoundMixer` (`1..7`).
- [x] В меню уровни показаны как `14..100%`; реально в микшер передаётся индекс `1..7`.
- [x] `Stereo` переключает стерео/моно сведение PSG без сброса эмулятора.
- [x] `PSG channels` выбирает панораму `ABC` или `ACB`.
- [ ] Проверить громкость, mono/stereo и ABC/ACB на PWM и I2S.

### Исправление 4.38.1 — Mute и восстановление фона Volume

- [x] В радиогруппу `Volume` добавлено значение `Mute (0%)`; штатные уровни `SoundMixer` остаются индексами `1..7`.
- [x] Пользовательское состояние mute отделено от временного приглушения при открытом меню и корректно применяется при его закрытии.
- [x] Пул сохранения фона каскадного меню увеличен с 32 до 64 КиБ: длинная страница `Volume` теперь помещается вместе с родительскими уровнями и восстанавливается по стрелке влево.
- [ ] Проверить `Mute`, все семь ненулевых уровней и возврат из `Volume` стрелкой влево на VGA и TV-out.


### Итерация 4.39 — меню Tape

Статус: подготовлена, ожидает проверки на железе.

- [x] Пустая страница `Tape` заменена рабочим меню управления файловым перенаправлением магнитофона.
- [x] `Redirect tape I/O` включает и отключает все десять CPU hooks штатной цепочки загрузки и сохранения.
- [x] `Load tape image...` заранее выбирает входной файл через существующий `TapeRedirector`; поддерживаемые фильтры и WAV/CSW-путь сохранены.
- [x] `Create new tape...` заранее создаёт выходной файл через существующий save-dialog и штатный выходной редиректор.
- [x] `Eject` закрывает и отсоединяет входной и выходной файлы; воспроизведение WAV/CSW также останавливается.
- [x] Внизу страницы независимо показываются текущие `Input:` и `Output:` с именем файла либо `empty`.
- [ ] Проверить загрузку `.rk/.cas`, создание выходного файла, WAV/CSW, переключение hooks и `Eject`.


### Итерация 4.40 — отключение PSG и HDD-контроллера

- [x] Пункт `PSG channels` заменён подменю `PSG`.
- [x] В подменю PSG добавлен динамический пункт `Enable` / `Disable`.
- [x] При отключённом PSG его порты возвращают `0xFF`, запись игнорируется, а звуковой источник PSG приглушается.
- [x] Выбор порядка каналов `ABC` / `ACB` сохранён; при отключённом PSG оба пункта недоступны.
- [x] В подменю HDD добавлен динамический пункт `Enable` / `Disable`.
- [x] При отключённом HDD-контроллере диапазон портов `0x50..0x5F` возвращает `0xFF` и игнорирует запись; подключённый образ не извлекается.
- [ ] Проверить отключение и повторное включение PSG и HDD без сброса машины.
