# Вектор-06Ц для Murmulator

## Техническое задание и план рефакторинга

Версия: 0.37

Документ объединяет техническое задание и чек-лист выполнения работ.

## Цель

Создать специализированную прошивку только для Вектор-06Ц.

### Основные требования

-   удалить мультиэмуляторность;
-   удалить стартовую страницу;
-   убрать динамическую загрузку конфигураций;
-   максимально упростить архитектуру;
-   убрать лишнюю виртуализацию;
-   обеспечить запуск без SD-карты;
-   использовать встроенные ROM;
-   оставить один физический видеорежим (800×600).

Работы выполняются в ветке **vector06c**.

## План работ

### Этап 1. Удаление мультиэмуляторности

-   [x] Удалить стартовую страницу.
-   [ ] Удалить остальные платформы.
-   [ ] Удалить динамическую загрузку конфигураций.
-   [ ] Оставить только Вектор-06Ц.
-   [ ] Перевести конфигурацию на статические структуры.

### Этап 2. Упрощение архитектуры

-   [ ] Удалить лишнюю виртуализацию.
-   [ ] Включить поля базовых классов в конечные структуры.
-   [ ] Удалить ненужные подсистемы.
-   [ ] Заменить косвенные вызовы прямыми.

### Этап 3. Видеотракт

-   [ ] Оставить только режим 800×600.
-   [ ] Удалить масштабирование.
-   [ ] Удалить смещение изображения.
-   [ ] Удалить NumLock-режим.
-   [ ] Удалить остальные физические видеорежимы.

### Этап 4. Меню

-   [ ] Иерархическое меню.
-   [ ] Общий файловый менеджер.
-   [ ] Snapshot.
-   [ ] Настройки.
-   [ ] ROM.
-   [ ] Носители.
-   [ ] Видео.
-   [ ] Звук.
-   [ ] Магнитофон.
-   [ ] Система.
-   [ ] О программе.


## Журнал выполнения

### Итерация 1 — прямой запуск Вектор-06Ц

-   [x] Удалён экран выбора платформы из `src/Main.cpp`.
-   [x] Удалены встроенный список платформ и CP866-таблица названий,
    использовавшиеся только стартовой страницей.
-   [x] В точку запуска жёстко передаётся `--platform vector`.
-   [ ] Остальные платформы и динамические конфигурации пока не удалялись: это
    отдельные последующие шаги этапа 1.

### Итерация 1.1 — исправление завершения стартового режима

-   [x] Восстановлено отключение временного framebuffer через
    `graphics_set_buffer(NULL, DISP_WIDTH, DISP_HEIGHT)` перед запуском PAL.
-   [x] Временный жёлтый буфер корректно отключён перед запуском PAL.
-   [x] Исправлен оставшийся на экране жёлтый прямоугольник, блокировавший
    отображение эмулятора после удаления меню.

### Итерация 1.2 — удаление временного framebuffer стартовой страницы

-   [x] Из `src/Main.cpp` полностью удалён `tmp_screen`, его выделение,
    заполнение жёлтым цветом и переключение framebuffer.
-   [x] `render_core()` теперь только инициализирует видеодрайвер; рабочий
    framebuffer устанавливает renderer Вектор-06Ц.
-   [x] Удалена скрытая зависимость `KorvetRenderer` от `tmp_screen`: пока
    Корвет остаётся в сборке, он снова выделяет и освобождает собственный
    framebuffer.

### Итерация 1.3 — единственный путь запуска Вектор-06Ц

-   [x] Из `src/Main.cpp` удалена искусственная передача `--platform vector`;
    запуск PAL теперь выполняется с единственным аргументом имени программы.
-   [x] `Emulation::processCmdLine()` больше не обрабатывает выбор платформы,
    старые платформенные ключи, `--conf-file` и выбор машины по расширению файла.
-   [x] Единственная создаваемая машина — `runPlatform("vector")`; штатная
    динамическая загрузка `vector.conf` пока сохранена до отдельного этапа.

### Итерация 1.4 — единственная зарегистрированная платформа

-   [x] Из `dist/emu80.conf` удалён каталог остальных платформ.
-   [x] Удалена отдельная платформа `vector.z80`: переключение 8080A/Z80 будет
    реализовано как настройка одной машины Вектор-06Ц.
-   [x] Удалены ассоциации расширений с платформами, утратившие смысл после
    фиксации единственной машины.
-   [x] В общем конфигурационном файле оставлена только регистрация
    `vector/vector.conf` с объектным именем `vector`.

## Архитектурные решения

### Процессор

-   8080A и Z80.
-   Переключение через меню.
-   После смены --- подтверждение, сохранение и рестарт виртуальной
    машины.

### Скорость CPU

-   Несколько фиксированных частот.
-   Unlimited.
-   Меняется без рестарта.

### Видео

-   Только 800×600.

### ROM

-   Штатные ROM встроены во flash.
-   Replacement ROM загружается с SD.
-   При отсутствии replacement используется встроенный ROM с
    предупреждением.

### Носители

-   2×FDD.
-   HDD.
-   Контроллеры отключаемые.
-   Образы можно извлекать.

#### Read Only

-   Галочка RO.
-   ScrLk переключает RO.
-   Повторное открытие уже используемого образа --- только RO.
-   Запись возвращает Write Protect.

### Магнитофон

-   Отключаемый.

### Звук

-   AY-3-8910 отключаемый.
-   Covox отключаемый.

### RTC

-   Начальное время = время сборки.
-   Далее часы идут программно.
-   Не сохраняется.

### Snapshot

-   Собственный формат.
-   12 слотов.
-   Left Win+F1..F12 --- сохранить.
-   Right Win+F1..F12 --- загрузить.
-   Полный дамп состояния устройств.
-   Без сжатия.
-   Блокирующее сохранение.
-   Во время операций: пауза, volume=0, VGA продолжает вывод.

### Меню

Разделы:

-   Процессор
-   Носители
-   ROM
-   Звук
-   Магнитофон
-   Снэпшоты
-   Видео
-   Система
-   О программе

### Горячие клавиши

-   Esc --- меню.
-   Alt+Del --- Soft Reset.
-   Ctrl+Alt+Del --- Hard Reset.
-   Left Win+F1..F12 --- сохранить snapshot.
-   Right Win+F1..F12 --- загрузить snapshot.


### Итерация 1.5 — прямой запуск конфигурации Вектора

- [x] Удалены выбор платформы по сохранённым настройкам и диалог выбора платформы.
- [x] `runPlatform()` принимает только имя `vector` и напрямую создаёт `vector/vector.conf`.
- [x] Общий конфигурационный файл больше не выбирает платформу: он только задаёт системные параметры и вызывает прямой запуск Вектора.
- [ ] Сам `vector/vector.conf` пока остаётся динамическим и требует файловой системы.

### Итерация 1.5.1 — восстановление системной инициализации

- [x] Возвращена обработка системной части `emu80.conf` перед запуском платформы.
- [x] Восстановлена инициализация `emulation.frequency`, частоты звука, FPS, VSync и сохранённых параметров из `emu80.opt`.
- [x] Устранён запуск видеотракта при неинициализированной частоте эмуляции.
- [ ] Системные параметры из `emu80.conf` предстоит перенести в статический код отдельной итерацией.


### Итерация 1.6 — очистка системного конфигурационного файла

- [x] Из `dist/emu80.conf` удалены `emu80.run`, `config.runFileName` и
  автоматическое подключение файла последней платформы.
- [x] Удалена оставшаяся регистрация платформы через `config.addPlatform`: Вектор
  уже запускается напрямую из `Emulation::processCmdLine()`.
- [x] Удалены закомментированные примеры запуска и создания других платформ.
- [x] Сохранены обязательные системные параметры, загрузка `emu80.opt` и вызов
  `emulation.processCmdLine`.

### Итерация 1.7 — отдельная базовая директория Вектор-06Ц

- [x] Базовая директория Pico PAL переключена с `/emu80/` на `/vector06c/`.
- [x] Начальная директория файлового диалога переключена с `/emu80` на `/vector06c`.
- [x] Исходный `dist/emu80.conf` восстановлен для полного мультиэмулятора.
- [x] Специализированный системный конфиг перенесён в `dist/vector06c/emu80.conf`.
- [x] Конфигурация и loader ROM Вектора скопированы в `dist/vector06c/vector/`.
- [ ] Динамическая загрузка конфигураций пока сохраняется; на этом этапе изменена только базовая директория.

### Итерация 1.8 — штатный loader ROM из flash

- [x] `Rom::Rom()` использует встроенный массив `vector_loader_rom` для пути `vector/loader.rom`.
- [x] Штатный loader Вектор-06Ц больше не читается с SD-карты.
- [x] Дублирующий `dist/vector06c/vector/loader.rom` удалён из специализированного дерева.
- [ ] `emu80.conf` и `vector.conf` пока остаются файловыми и всё ещё требуют SD-карту.

### Итерация 1.9 — встроенная конфигурация машины

- [x] `vector/vector.conf` встроен во flash как `src/pico/vector06c_config.h`.
- [x] Для Pico `Platform` передаёт встроенный текст непосредственно `ConfigReader`, без чтения файла с SD.
- [x] `dist/vector06c/vector/vector.conf` удалён; парсер конфигураций пока сохранён как промежуточный механизм.
- [ ] Следующий этап — заменить разбор встроенного текста статическим созданием объектов и связей.

### Итерация 1.10 — прямая системная инициализация

- [x] Удалён ошибочный `vector06c_system_config.h` с текстовой конфигурацией.
- [x] Обязательные системные параметры задаются напрямую в конструкторе `Emulation` без `ConfigReader`.
- [x] Напрямую установлены частота эмуляции, частота звука, громкость, FPS, VSync и параметры отладчика.
- [x] `processCmdLine()` вызывается непосредственно после системной инициализации.
- [x] Зависимость запуска от `emu80.conf` и `emu80.opt` отсутствует.

### Итерация 1.11 — удаление обработки командной строки при запуске

- [x] `Emulation::processCmdLine()` больше не разбирает параметры запуска, загрузки файлов и подключения носителей.
- [x] Удалена поддержка `post-conf`, утратившая смысл после отказа от динамических конфигураций.
- [x] Удалены командные параметры `run`, `load`, `disk-a`...`disk-d`, `hdd`, `edd` и `edd2` из пути создания машины.
- [x] Запуск теперь непосредственно создаёт единственную платформу `vector`.
- [x] Из `Emulation::setProperty()` удалены конфигурационные команды `runPlatform` и `processCmdLine`.

### Итерация 1.12 — прямой запуск из конструктора Emulation

- [x] Удалён промежуточный метод `Emulation::processCmdLine()`.
- [x] Конструктор `Emulation` непосредственно вызывает `runPlatform("vector")` после системной инициализации.
- [x] Из `Emulation.h` удалены объявление `processCmdLine()` и больше не используемый флаг `m_platformCreatedFromCmdLine`.

### Итерация 1.12.1 — восстановление создания платформы

- [x] После удаления `processCmdLine()` восстановлен прямой вызов `runPlatform("vector")` в конструкторе `Emulation`.
- [x] Вызов выполняется до `checkPlatforms()`, поэтому список платформ заполняется до проверки.
- [x] Устранён запуск видеотракта без создания и запуска машины Вектор-06Ц.

### Итерация 1.13 — удаление смены платформы и конфигурации

- [x] Из `Emulation::sysReq()` удалён обработчик `SR_CHPLATFORM`, создававший произвольную платформу через `EmuConfig`.
- [x] Удалён обработчик `SR_CHCONFIG`, перезапускавший машину через зарегистрированный список конфигураций.
- [x] Горячие команды смены платформы и конфигурации больше не могут вернуть мультиэмуляторный путь во время работы.
- [ ] `EmuConfig` пока сохраняется для существующего окна настроек и справки.

### Итерация 1.14 — единственная платформа без имени

- [x] `runPlatform()` больше не принимает имя платформы и безусловно создаёт Вектор-06Ц.
- [x] Удалена проверка имени `vector`, утратившая смысл в специализированной ветке.
- [x] `newPlatform()` пока сохранён для существующих внешних вызовов, но всегда пересоздаёт единственную машину.

### Итерация 1.15 — удаление внешнего API смены платформы

- [x] Удалён `Emulation::newPlatform()`: повторное создание машины по внешнему имени больше невозможно.
- [x] Удалён общий вызов `emuSelectPlatform()` и его объявление.
- [x] Qt- и WASM-обёртки больше не вызывают API выбора платформы.
- [x] Единственный путь создания Вектор-06Ц остаётся в конструкторе `Emulation`.

### Итерация 1.16 — прямая проверка создания Вектора

- [x] Удалён `Emulation::checkPlatforms()` — проверка списка платформ больше не используется.
- [x] Конструктор проверяет результат единственного вызова `runPlatform()` непосредственно.
- [x] При ошибке создания Вектора выводится сообщение и запрашивается завершение работы.
- [x] Удалено объявление `checkPlatforms()` из `Emulation.h`.

### Итерация 1.17 — единственная активная платформа

- [x] Удалён список `m_platformList`: экземпляр Вектор-06Ц хранится только в `m_activePlatform`.
- [x] Удалены `platformByWindow()` и `m_lastActivePlatform`; ввод, фокус и загрузка файлов работают напрямую с единственным окном Вектора.
- [x] Деструктор и `SR_CLOSE` напрямую удаляют единственную активную платформу.
- [x] `addChild()` сохранён как обязательный интерфейс `EmuObject`, но теперь принимает только единственную платформу.

### Итерация 1.18 — удаление одноразовой прослойки запуска

- [x] Удалён метод `runPlatform()`, использовавшийся только один раз при создании машины.
- [x] Единственный `Platform` Вектор-06Ц создаётся непосредственно в конструкторе `Emulation`.
- [x] Проверка наличия окна и обработка ошибки создания сохранены без промежуточного вызова.

### Итерация 1.19 — удаление зависимости от CmdLine

- [x] Конструктор `Emulation` больше не принимает и не хранит объект `CmdLine`.
- [x] Из `Main.cpp` удалено создание пустого `CmdLine`; `Emulation` создаётся напрямую.
- [x] Удалены неиспользуемые включения и предварительное объявление `CmdLine` из `Emulation`.
- [x] Файлы `CmdLine.*` и их записи в сборочных проектах удалены отдельной итерацией.

### Итерация 1.20 — удаление подсистемы CmdLine

- [x] Удалены `src/CmdLine.cpp` и `src/CmdLine.h`: после прямого запуска Вектора ссылок на класс больше нет.
- [x] Удалены записи `CmdLine.cpp/.h` из проектов Qt и Code::Blocks.
- [x] Pico CMake больше не включает `CmdLine.cpp` через общий `file(GLOB ...)`, поскольку файл удалён.

### Итерация 1.21 — удаление неподдерживаемых frontend-сборок

- [x] Удалены каталоги `src/qt`, `src/sdl`, `src/wx`, `src/wasm` и `src/lite`.
- [x] Удалены проекты Qt/Code::Blocks, старые desktop/WASM Makefile и desktop CI.
- [x] Удалён резервный `src/Main.bak`, относившийся к старой desktop-точке входа.
- [x] README сокращён до поддерживаемой Pico SDK/CMake-сборки специализированной прошивки.
- [x] Неподдерживаемые frontend-исходники больше не попадают под рекурсивный CMake glob и не требуют поддержания консистентности.

### Итерация 1.22 — статическое создание окна

- [x] `vector06c_config` подключается и используется безусловно.
- [x] Удалены `#ifdef PICO_RP2350`, неактуальный `#else` и файловый fallback.
- [x] `EmuWindow` создаётся непосредственно в `Platform` с фиксированными параметрами 800×600.
- [x] Блок окна удалён из встроенного и справочного `vector.conf`.

### Итерация 1.23 — статические RAM и ROM

- [x] Основная RAM объёмом 64 КиБ создаётся напрямую в `Platform`.
- [x] Loader ROM объёмом 32 КиБ создаётся напрямую в `Platform`.
- [x] `Rom` безусловно использует встроенный `vector_loader_rom` для `vector/loader.rom`.
- [x] Объявления `Ram ram` и `Rom rom` удалены из встроенного и справочного `vector.conf`.

### Итерация 1.24 — статическое создание CPU

- [x] `Cpu8080` создаётся непосредственно в `Platform`.
- [x] Частота 3 МГц и стартовый адрес `0x0000` задаются напрямую.
- [x] Объявление CPU и его базовые параметры удалены из встроенного и справочного `vector.conf`.
- [x] Оставшийся конфиг продолжает связывать объект `cpu` с адресными пространствами, ядром и ожиданиями.

### Итерация 1.25 — статические адресные пространства

- [x] `VectorAddrSpace` создаётся непосредственно в `Platform` и получает RAM, ROM и CPU без парсера.
- [x] Портовое `AddrSpace` создаётся непосредственно в `Platform`.
- [x] CPU напрямую подключается к обоим адресным пространствам.
- [x] Объявления адресных пространств и базовые связи удалены из встроенного и справочного `vector.conf`; связи с рендерером и RAM-диском пока остаются в конфиге.

### Итерация 1.26 — статические видеорендерер и ядро

- [x] `VectorRenderer` создаётся непосредственно в `Platform` и напрямую получает основную RAM.
- [x] Режим отображения видимой области задаётся прямым методом без конфигурационного парсера.
- [x] `VectorAddrSpace` напрямую подключается к рендереру.
- [x] `VectorCore` создаётся непосредственно и связывается с окном, рендерером и CPU.
- [x] Объявления рендерера и ядра и их базовые связи удалены из встроенного и справочного `vector.conf`.

### Итерация 1.26.1 — восстановление связи объектов с платформой

- [x] `Platform::addChild()` безусловно назначает добавляемому объекту текущую платформу.
- [x] Статически созданные CPU, адресные пространства, рендерер и ядро получают тот же `m_platform`, который ранее назначал `ConfigReader`.
- [x] Исправлен срыв эмуляции при первом кадровом событии `VectorCore::vrtc()`.

### Итерация 1.27 — статические клавиатура и ввод текста

- [x] `VectorKeyboard` создаётся непосредственно в `Platform`.
- [x] `VectorKbdLayout` создаётся непосредственно и без парсера переводится в режим QWERTY.
- [x] `KbdTapper` создаётся непосредственно; времена нажатия, отпускания и задержка после Enter задаются прямыми методами.
- [x] Объявления клавиатуры, раскладки и `KbdTapper` удалены из встроенного и справочного `vector.conf`.

### Итерация 1.28 — статический основной ВВ55

- [x] `VectorPpi8255Circuit`, магнитофонный `GeneralSoundSource`, основной `Ppi8255` и `AddrSpaceInverter` создаются непосредственно в `Platform`.
- [x] Обвязка ВВ55 напрямую связана с рендерером, клавиатурой и магнитофонным источником звука.
- [x] Основной ВВ55 напрямую подключён к диапазону портов `0x00..0x03`.
- [x] Соответствующий блок удалён из встроенного и справочного `vector.conf`.

### Итерация 1.29 — статический регистр палитры

- [x] `VectorColorRegister` создаётся непосредственно в `Platform`.
- [x] Регистр напрямую связывается с уже статическим `VectorRenderer`.
- [x] Регистр напрямую подключается к диапазону портов `0x0C..0x0F`.
- [x] Соответствующий блок удалён из встроенного и справочного `vector.conf`.

### Итерация 1.30 — статические дополнительные порты и звук

- [x] Второй `Ppi8255`, его `AddrSpaceInverter`, `VectorPpi8255Circuit2` и 7-битный `Covox` создаются непосредственно в `Platform`.
- [x] Covox переводится в отрицательную полярность прямым вызовом, а второй ВВ55 подключается к портам `0x04..0x07`.
- [x] `Pit8253` с частотой 1,5 МГц, его звуковой источник и инвертор портов создаются напрямую; таймер подключён к `0x08..0x0B`.
- [x] `Psg3910` с частотой 1,75 МГц и `Psg3910SoundSource` создаются напрямую; AY подключён к `0x14..0x15`.
- [x] Все три конфигурационных блока удалены из встроенного и справочного `vector.conf`.

### Итерация 1.31 — статическая дисковая портовая обвязка

- [x] `Fdc1793`, его инвертор адресов и `VectorFddControlRegister` создаются непосредственно в `Platform`.
- [x] ВГ93 подключён к портам `0x18..0x1B`, регистр выбора дисковода — к `0x1C`.
- [x] `AtaDrive` создаётся напрямую и переводится в геометрию Вектора без конфигурационного парсера.
- [x] `VectorHddRegisters` создаётся напрямую, связывается с ATA-устройством и подключается к портам `0x50..0x5F`.
- [x] Образы дискет и HDD остаются в конфиге и по именам подключаются к уже статическим контроллерам.
- [x] Соответствующие объявления контроллеров удалены из встроенного и справочного `vector.conf`.

## Критерии готовности

-   [ ] Работает без SD.
-   [ ] Только Вектор-06Ц.
-   [ ] Один видеорежим.
-   [ ] Статическая конфигурация.
-   [ ] Полностью работает меню.
-   [ ] Работают snapshot.
-   [ ] Все ROM встроены.
-   [ ] Мультиплатформенность удалена.
