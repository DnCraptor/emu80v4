# Вектор-06Ц для Murmulator

## Техническое задание и план рефакторинга

Версия: 0.17

Документ объединяет техническое задание и чек-лист выполнения работ.

## Цель

Создать специализированную прошивку только для Вектор-06Ц.

### Основные требования

-   удалить мультиэмуляторность;
-   удалить стартовую страницу;
-   убрать динамическую загрузку конфигураций;
-   максимально упростить архитектуру;
-   убрать лишнюю виртуализацию;
-   обеспечить запуск без SD-карты;
-   использовать встроенные ROM;
-   оставить один физический видеорежим (800×600).

Работы выполняются в ветке **vector06c**.

## План работ

### Этап 1. Удаление мультиэмуляторности

-   [x] Удалить стартовую страницу.
-   [ ] Удалить остальные платформы.
-   [ ] Удалить динамическую загрузку конфигураций.
-   [ ] Оставить только Вектор-06Ц.
-   [ ] Перевести конфигурацию на статические структуры.

### Этап 2. Упрощение архитектуры

-   [ ] Удалить лишнюю виртуализацию.
-   [ ] Включить поля базовых классов в конечные структуры.
-   [ ] Удалить ненужные подсистемы.
-   [ ] Заменить косвенные вызовы прямыми.

### Этап 3. Видеотракт

-   [ ] Оставить только режим 800×600.
-   [ ] Удалить масштабирование.
-   [ ] Удалить смещение изображения.
-   [ ] Удалить NumLock-режим.
-   [ ] Удалить остальные физические видеорежимы.

### Этап 4. Меню

-   [ ] Иерархическое меню.
-   [ ] Общий файловый менеджер.
-   [ ] Snapshot.
-   [ ] Настройки.
-   [ ] ROM.
-   [ ] Носители.
-   [ ] Видео.
-   [ ] Звук.
-   [ ] Магнитофон.
-   [ ] Система.
-   [ ] О программе.


## Журнал выполнения

### Итерация 1 — прямой запуск Вектор-06Ц

-   [x] Удалён экран выбора платформы из `src/Main.cpp`.
-   [x] Удалены встроенный список платформ и CP866-таблица названий,
    использовавшиеся только стартовой страницей.
-   [x] В точку запуска жёстко передаётся `--platform vector`.
-   [ ] Остальные платформы и динамические конфигурации пока не удалялись: это
    отдельные последующие шаги этапа 1.

### Итерация 1.1 — исправление завершения стартового режима

-   [x] Восстановлено отключение временного framebuffer через
    `graphics_set_buffer(NULL, DISP_WIDTH, DISP_HEIGHT)` перед запуском PAL.
-   [x] Временный жёлтый буфер корректно отключён перед запуском PAL.
-   [x] Исправлен оставшийся на экране жёлтый прямоугольник, блокировавший
    отображение эмулятора после удаления меню.

### Итерация 1.2 — удаление временного framebuffer стартовой страницы

-   [x] Из `src/Main.cpp` полностью удалён `tmp_screen`, его выделение,
    заполнение жёлтым цветом и переключение framebuffer.
-   [x] `render_core()` теперь только инициализирует видеодрайвер; рабочий
    framebuffer устанавливает renderer Вектор-06Ц.
-   [x] Удалена скрытая зависимость `KorvetRenderer` от `tmp_screen`: пока
    Корвет остаётся в сборке, он снова выделяет и освобождает собственный
    framebuffer.

### Итерация 1.3 — единственный путь запуска Вектор-06Ц

-   [x] Из `src/Main.cpp` удалена искусственная передача `--platform vector`;
    запуск PAL теперь выполняется с единственным аргументом имени программы.
-   [x] `Emulation::processCmdLine()` больше не обрабатывает выбор платформы,
    старые платформенные ключи, `--conf-file` и выбор машины по расширению файла.
-   [x] Единственная создаваемая машина — `runPlatform("vector")`; штатная
    динамическая загрузка `vector.conf` пока сохранена до отдельного этапа.

### Итерация 1.4 — единственная зарегистрированная платформа

-   [x] Из `dist/emu80.conf` удалён каталог остальных платформ.
-   [x] Удалена отдельная платформа `vector.z80`: переключение 8080A/Z80 будет
    реализовано как настройка одной машины Вектор-06Ц.
-   [x] Удалены ассоциации расширений с платформами, утратившие смысл после
    фиксации единственной машины.
-   [x] В общем конфигурационном файле оставлена только регистрация
    `vector/vector.conf` с объектным именем `vector`.

## Архитектурные решения

### Процессор

-   8080A и Z80.
-   Переключение через меню.
-   После смены --- подтверждение, сохранение и рестарт виртуальной
    машины.

### Скорость CPU

-   Несколько фиксированных частот.
-   Unlimited.
-   Меняется без рестарта.

### Видео

-   Только 800×600.

### ROM

-   Штатные ROM встроены во flash.
-   Replacement ROM загружается с SD.
-   При отсутствии replacement используется встроенный ROM с
    предупреждением.

### Носители

-   2×FDD.
-   HDD.
-   Контроллеры отключаемые.
-   Образы можно извлекать.

#### Read Only

-   Галочка RO.
-   ScrLk переключает RO.
-   Повторное открытие уже используемого образа --- только RO.
-   Запись возвращает Write Protect.

### Магнитофон

-   Отключаемый.

### Звук

-   AY-3-8910 отключаемый.
-   Covox отключаемый.

### RTC

-   Начальное время = время сборки.
-   Далее часы идут программно.
-   Не сохраняется.

### Snapshot

-   Собственный формат.
-   12 слотов.
-   Left Win+F1..F12 --- сохранить.
-   Right Win+F1..F12 --- загрузить.
-   Полный дамп состояния устройств.
-   Без сжатия.
-   Блокирующее сохранение.
-   Во время операций: пауза, volume=0, VGA продолжает вывод.

### Меню

Разделы:

-   Процессор
-   Носители
-   ROM
-   Звук
-   Магнитофон
-   Снэпшоты
-   Видео
-   Система
-   О программе

### Горячие клавиши

-   Esc --- меню.
-   Alt+Del --- Soft Reset.
-   Ctrl+Alt+Del --- Hard Reset.
-   Left Win+F1..F12 --- сохранить snapshot.
-   Right Win+F1..F12 --- загрузить snapshot.


### Итерация 1.5 — прямой запуск конфигурации Вектора

- [x] Удалены выбор платформы по сохранённым настройкам и диалог выбора платформы.
- [x] `runPlatform()` принимает только имя `vector` и напрямую создаёт `vector/vector.conf`.
- [x] Общий конфигурационный файл больше не выбирает платформу: он только задаёт системные параметры и вызывает прямой запуск Вектора.
- [ ] Сам `vector/vector.conf` пока остаётся динамическим и требует файловой системы.

### Итерация 1.5.1 — восстановление системной инициализации

- [x] Возвращена обработка системной части `emu80.conf` перед запуском платформы.
- [x] Восстановлена инициализация `emulation.frequency`, частоты звука, FPS, VSync и сохранённых параметров из `emu80.opt`.
- [x] Устранён запуск видеотракта при неинициализированной частоте эмуляции.
- [ ] Системные параметры из `emu80.conf` предстоит перенести в статический код отдельной итерацией.


### Итерация 1.6 — очистка системного конфигурационного файла

- [x] Из `dist/emu80.conf` удалены `emu80.run`, `config.runFileName` и
  автоматическое подключение файла последней платформы.
- [x] Удалена оставшаяся регистрация платформы через `config.addPlatform`: Вектор
  уже запускается напрямую из `Emulation::processCmdLine()`.
- [x] Удалены закомментированные примеры запуска и создания других платформ.
- [x] Сохранены обязательные системные параметры, загрузка `emu80.opt` и вызов
  `emulation.processCmdLine`.

### Итерация 1.7 — отдельная базовая директория Вектор-06Ц

- [x] Базовая директория Pico PAL переключена с `/emu80/` на `/vector06c/`.
- [x] Начальная директория файлового диалога переключена с `/emu80` на `/vector06c`.
- [x] Исходный `dist/emu80.conf` восстановлен для полного мультиэмулятора.
- [x] Специализированный системный конфиг перенесён в `dist/vector06c/emu80.conf`.
- [x] Конфигурация и loader ROM Вектора скопированы в `dist/vector06c/vector/`.
- [ ] Динамическая загрузка конфигураций пока сохраняется; на этом этапе изменена только базовая директория.

### Итерация 1.8 — штатный loader ROM из flash

- [x] `Rom::Rom()` использует встроенный массив `vector_loader_rom` для пути `vector/loader.rom`.
- [x] Штатный loader Вектор-06Ц больше не читается с SD-карты.
- [x] Дублирующий `dist/vector06c/vector/loader.rom` удалён из специализированного дерева.
- [ ] `emu80.conf` и `vector.conf` пока остаются файловыми и всё ещё требуют SD-карту.

### Итерация 1.9 — встроенная конфигурация машины

- [x] `vector/vector.conf` встроен во flash как `src/pico/vector06c_config.h`.
- [x] Для Pico `Platform` передаёт встроенный текст непосредственно `ConfigReader`, без чтения файла с SD.
- [x] `dist/vector06c/vector/vector.conf` удалён; парсер конфигураций пока сохранён как промежуточный механизм.
- [ ] Следующий этап — заменить разбор встроенного текста статическим созданием объектов и связей.

### Итерация 1.10 — прямая системная инициализация

- [x] Удалён ошибочный `vector06c_system_config.h` с текстовой конфигурацией.
- [x] Обязательные системные параметры задаются напрямую в конструкторе `Emulation` без `ConfigReader`.
- [x] Напрямую установлены частота эмуляции, частота звука, громкость, FPS, VSync и параметры отладчика.
- [x] `processCmdLine()` вызывается непосредственно после системной инициализации.
- [x] Зависимость запуска от `emu80.conf` и `emu80.opt` отсутствует.

### Итерация 1.11 — удаление обработки командной строки при запуске

- [x] `Emulation::processCmdLine()` больше не разбирает параметры запуска, загрузки файлов и подключения носителей.
- [x] Удалена поддержка `post-conf`, утратившая смысл после отказа от динамических конфигураций.
- [x] Удалены командные параметры `run`, `load`, `disk-a`...`disk-d`, `hdd`, `edd` и `edd2` из пути создания машины.
- [x] Запуск теперь непосредственно создаёт единственную платформу `vector`.
- [x] Из `Emulation::setProperty()` удалены конфигурационные команды `runPlatform` и `processCmdLine`.

### Итерация 1.12 — прямой запуск из конструктора Emulation

- [x] Удалён промежуточный метод `Emulation::processCmdLine()`.
- [x] Конструктор `Emulation` непосредственно вызывает `runPlatform("vector")` после системной инициализации.
- [x] Из `Emulation.h` удалены объявление `processCmdLine()` и больше не используемый флаг `m_platformCreatedFromCmdLine`.

### Итерация 1.12.1 — восстановление создания платформы

- [x] После удаления `processCmdLine()` восстановлен прямой вызов `runPlatform("vector")` в конструкторе `Emulation`.
- [x] Вызов выполняется до `checkPlatforms()`, поэтому список платформ заполняется до проверки.
- [x] Устранён запуск видеотракта без создания и запуска машины Вектор-06Ц.

## Критерии готовности

-   [ ] Работает без SD.
-   [ ] Только Вектор-06Ц.
-   [ ] Один видеорежим.
-   [ ] Статическая конфигурация.
-   [ ] Полностью работает меню.
-   [ ] Работают snapshot.
-   [ ] Все ROM встроены.
-   [ ] Мультиплатформенность удалена.
